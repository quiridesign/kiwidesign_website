import {
  D as Kr,
  I as Lr,
  J as Tj,
  K as wr,
  L as Jr,
  M as De,
  P as xe,
  Pa as zr,
  Qa as Wr,
  Ra as Yr,
  b as Dr,
  fa as Hr,
  g as ve,
  ga as Ne,
  h as xr,
  ha as Vr,
  i as Nr,
  ja as Cj,
} from "chunk-JDKTV32K.mjs";
import { H as Ke, K as qj, L as j7, P as e7, Q as t7, R as o7, q as Nj } from "chunk-JP2GKNMH.mjs";
import {
  Ac as Gr,
  C as Oj,
  D as ir,
  Da as Mj,
  Ef as Or,
  Hc as mr,
  He as Ir,
  Hf as Mr,
  If as vr,
  Jc as Rr,
  Kc as Fr,
  Ke as gr,
  Me as hr,
  N as ar,
  Of as Pr,
  Se as _r,
  Tg as Te,
  Ue as vj,
  Ve as br,
  We as Sr,
  Wg as Ce,
  bc as Br,
  ga as sr,
  ie as fr,
  ja as kr,
  ka as lr,
  lg as Pj,
  nh as qe,
  oh as A7,
  wf as yr,
  yb as $r,
  yc as SA,
  zb as dr,
  zc as pr,
} from "chunk-KN3JEK6R.mjs";
import { B as Zr } from "chunk-FD4BH2A6.mjs";
import { a as At } from "chunk-ZGSYAT7B.mjs";
import { c as Dj } from "chunk-5LPGVEDR.mjs";
import { b as X, c as cr } from "chunk-G4AKBPZO.mjs";
import { a as r7 } from "chunk-V3C5CSFI.mjs";
import { c as Cr } from "chunk-QH5BKXZN.mjs";
import { a as Qr } from "chunk-XUP3VNZ4.mjs";
import { A as xj, B as Xr, t as qr } from "chunk-XLD7EJQD.mjs";
import { A as Pe, f as Er } from "chunk-RKH4M4Z3.mjs";
import { a as Tr } from "chunk-HA2BG6ZV.mjs";
import {
  Aa as Ut,
  Ba as Xt,
  Dc as Zt,
  Ib as zt,
  Od as jr,
  Qd as er,
  W as Se,
  Xd as tr,
  ae as rr,
  fe as or,
  pe as nr,
  ya as Vt,
  yb as Qt,
} from "chunk-K6WOFN2G.mjs";
import { a as Ar } from "chunk-XO3S5WOW.mjs";
import { Bb as KA, a as tt, nc as Wt, qc as Yt } from "chunk-BOAU3TCY.mjs";
import { a as ej } from "chunk-M5XSLRLD.mjs";
import { f as L } from "chunk-2XMXWGLC.mjs";
import { Ge as Oe } from "chunk-Z5RAPOF6.mjs";
import { a as yA } from "chunk-WCTCEL2Q.mjs";
import { g as Ur } from "chunk-NMSN6DQX.mjs";
import { a as Me } from "chunk-CNMLD24G.mjs";
import {
  $G as be,
  $e as QA,
  $j as pA,
  AC as Re,
  AG as ge,
  AL as jj,
  Ac as I6,
  Ad as P,
  CC as pt,
  CG as _j,
  Cc as g6,
  DC as ut,
  Dc as ie,
  Du as jt,
  GC as Gt,
  Hw as ot,
  IC as mt,
  IF as St,
  IG as he,
  IJ as yj,
  Iw as nt,
  J as nA,
  JC as Rt,
  JJ as ye,
  LG as _e,
  Lq as Ge,
  Ol as L6,
  PF as yt,
  PG as bj,
  QC as Ft,
  QF as Ot,
  Qd as b6,
  Qr as U6,
  RG as vt,
  Rd as S6,
  Rj as D6,
  Sd as y6,
  Sl as w6,
  Ss as W6,
  TC as Ij,
  TG as Pt,
  Tc as ae,
  Td as O6,
  UE as _t,
  UG as Tt,
  Ud as le,
  Ui as C6,
  Ur as X6,
  VC as ft,
  WC as It,
  WG as Ct,
  WH as Dt,
  WK as kA,
  Wd as ce,
  Wm as de,
  XF as hj,
  XI as Nt,
  XK as Jt,
  Xd as M6,
  Xm as Be,
  Xr as Q6,
  YE as gj,
  YK as Ht,
  Yd as v6,
  Yt as WA,
  Zc as iA,
  Zd as P6,
  Zk as q6,
  Zu as et,
  Zw as it,
  _A as YA,
  _C as gt,
  _I as qt,
  _t as Y6,
  ac as TA,
  ay as kt,
  bH as tA,
  bs as NA,
  cd as h6,
  ck as Gj,
  d as u6,
  dk as x6,
  dl as K6,
  du as Z6,
  fd as _6,
  fx as me,
  gd as CA,
  gs as mj,
  hC as Rj,
  hJ as eA,
  hc as XA,
  hd as se,
  he as T6,
  iC as Fj,
  iD as ZA,
  id as ke,
  jC as fj,
  jD as Fe,
  kD as fe,
  kc as f6,
  kn as J6,
  kx as at,
  lB as lt,
  lC as ct,
  lc as Bj,
  lk as N6,
  ln as pe,
  lx as st,
  m as G6,
  md as jA,
  nC as $t,
  nb as F6,
  nd as DA,
  oD as Ie,
  oe as uj,
  pD as ht,
  pJ as Sj,
  pd as aA,
  pe as U,
  rG as Mt,
  rJ as Kt,
  sC as dt,
  sJ as Lt,
  sp as xA,
  to as H6,
  tp as V6,
  uI as xt,
  ud as bA,
  vd as v,
  vn as zA,
  vo as sA,
  wd as pj,
  wo as ue,
  ww as rt,
  xC as Bt,
  xF as bt,
  xJ as qA,
  yJ as Aj,
  zJ as wt,
  zd as Y,
} from "chunk-IXMBKHRL.mjs";
import { b as Et } from "chunk-6FIQ6K2E.mjs";
import { a as $e } from "chunk-UYIYJ4FN.mjs";
import { o as D } from "chunk-ZA5GQ2BE.mjs";
import { a as ur } from "chunk-F3T3XR66.mjs";
import { d as Ee, f as uA } from "chunk-H4RI37WJ.mjs";
import { a as R6 } from "chunk-5WDLMAA7.mjs";
import { b as p6 } from "chunk-AFIDN3ND.mjs";
import { ng as z6 } from "chunk-P3ZYSNII.mjs";
import { a as m6 } from "chunk-JTCAKYEM.mjs";
import {
  b as m,
  d as oe,
  e as W,
  f as AA,
  k as VA,
  m as _A,
  n as UA,
  p as ne,
} from "chunk-LA34HORX.mjs";
import { j as hA } from "chunk-FZGWLIPZ.mjs";
import { k as re } from "chunk-NQLCAFDF.mjs";
import {
  Aa as Ej,
  Ba as G,
  Da as E6,
  Ga as ee,
  La as B6,
  X as z,
  Ya as te,
  Z as c6,
  ia as BA,
  k as oA,
  ra as $6,
  sa as q,
  ta as K,
  ua as J,
  wa as gA,
  xa as HA,
  za as d6,
} from "chunk-FVJDO2XD.mjs";
import { b as d, c as N } from "chunk-4JY5UMT2.mjs";
import {
  e as l6,
  f as cj,
  g as $j,
  h as H,
  i as EA,
  j as u,
  k as C,
  l as V,
  m as dj,
} from "chunk-WLHSDIGQ.mjs";
var n7 = [xA.video, xA.youtube, xA.vimeo, xA.codeblock, xA.twitter];
function ni(t, A) {
  let j = G(A);
  return K(j)
    ? j.type === "canvasComponent"
    : q(j)
      ? t.tree.getNodeWithTrait(j.moduleId, U)?.type === "canvasComponent"
      : !1;
}
async function ii(t, A) {
  let j = G(A);
  q(j) && (await t.stores.modulesStore.addExternalModulesToProject([j], { onTreeUpdate() {} }));
}
var i7 = new WeakMap();
async function ai(t) {
  let { modules: A } = await t.stores.modulesStore.lookUpModules(n7.map(V6)),
    j = [];
  for (let e = 0; e < A.length; e++) {
    let r = A[e];
    d(r, "Module must exist");
    let o = n7[e];
    (d(o, "Module component must exist"),
      i7.set(o, r),
      d(r.files.module, "Module must have a module file"),
      j.push(gA(r.id, r.saveId, r.files.module, o.exportSpecifier ?? "default")));
  }
  await t.stores.modulesStore.preloadExternalModules(j);
}
function si(t, A) {
  let j = i7.get(A);
  d(j, "Preloaded module must exist");
  let e = t.getNodeWithTrait(j.id, U);
  return e
    ? e.codeComponentIdentifier
    : (d(j.files.module, "Module must have a module file"),
      gA(j.id, j.saveId, j.files.module, A.exportSpecifier ?? "default").value);
}
var a7 = "https://app.framerstatic.com/framer_compiler_bg-UGK2KYHD.wasm";
var go = { track: !0 };
function s7(t) {
  let A = { ...t, event: "external_component_insert" };
  p6(A, go);
}
var lA = BA("compiler");
async function Ii() {
  await tj();
}
function k7(t) {
  return {
    code: `const err = new Error(${JSON.stringify(String(t))}); err.name = "CompilationError"; throw err;`,
    sourceMap: void 0,
    annotations: {},
    exportedNames: [],
    reExportedModules: [],
    imports: { absolute: [], relative: [], bare: [] },
  };
}
function l7(t, A, j) {
  let e = { ...A.exports };
  j &&
    ((e.__FramerMetadata__ = { type: "variable" }),
    (t = t.concat(
      `
export const __FramerMetadata__ = `,
      JSON.stringify({ exports: e })
    )));
  let r = Object.entries(e)
      .filter(([, i]) => i.type !== "tsType")
      .map(([i]) => i),
    o = {};
  for (let [i, n] of Object.entries(A.exports)) n.annotations && (o[i] = n.annotations);
  return { moduleCode: t, annotations: o, exportedNames: r };
}
async function c7({ localId: t, name: A, source: j, includeSourceMap: e = !0 }) {
  let r = await tj();
  try {
    let {
        code: o,
        metadata: i,
        map: n,
      } = await r.transformSync(j, {
        fileName: yA(A).source,
        framerContractVersion: 1,
        mediaType: "tsx",
        minify: !1,
        compress: !1,
        mangle: !1,
        sourceMap: e,
        useReactRefresh: !0,
      }),
      s = `
if (!window.$RefreshReg$) throw new Error("React refresh preamble was not loaded. Something is wrong.");
const prevRefreshReg = window.$RefreshReg$;
const prevRefreshSig = window.$RefreshSig$;
window.$RefreshReg$ = window.reactRefreshRuntime.getRefreshReg("${t}");
window.$RefreshSig$ = window.reactRefreshRuntime.createSignatureFunctionForTransform;

${o}

window.$RefreshReg$ = prevRefreshReg;
window.$RefreshSig$ = prevRefreshSig;`,
      { moduleCode: a, annotations: k, exportedNames: l } = l7(s, i, !0);
    return {
      code: a,
      sourceMap: n ?? void 0,
      imports: i.requestedModules,
      annotations: k,
      exportedNames: l,
      reExportedModules: i.reExportedModules,
    };
  } catch (o) {
    let i = `Failed to compile development module ${A}: ${o}`;
    return (
      lA.debug(i, "(enable trace logging to see full source)"),
      lA.trace(j),
      t.startsWith("codeFile/") ||
        (lA.reportCriticalError(new Error(i, { cause: o }), { moduleName: A, moduleSource: j }),
        L("application_error", { message: i, area: "compiler" })),
      k7(o)
    );
  }
}
async function gi(t, A) {
  await (
    await tj()
  ).transformSync(A, {
    fileName: yA(t).source,
    framerContractVersion: 1,
    mediaType: "tsx",
    minify: !0,
    compress: !1,
    mangle: !1,
    sourceMap: !1,
    useReactRefresh: !1,
  });
}
async function $7({
  name: t,
  source: A,
  type: j,
  includeSourceMap: e = !0,
  addFramerMetadata: r = !0,
  telemetrySession: o,
}) {
  lA.debug("Compiling module", t, "; with source map:", e);
  let i = performance.now(),
    n = await tj(),
    s = o?.start("transform"),
    a = j === "collection";
  try {
    let {
        code: k,
        map: l,
        metadata: c,
      } = await n.transformSync(A, {
        fileName: yA(t).source,
        framerContractVersion: 1,
        mediaType: "tsx",
        minify: !0,
        compress: a,
        mangle: a,
        sourceMap: e,
        useReactRefresh: !1,
      }),
      { moduleCode: $, annotations: B, exportedNames: E } = l7(k, c, r);
    return {
      code: $,
      sourceMap: l ?? void 0,
      imports: c.requestedModules,
      annotations: B,
      exportedNames: E,
      reExportedModules: c.reExportedModules,
    };
  } catch (k) {
    let l = `Failed to compile module ${t}: ${k}`;
    return (
      lA.debug(l, "(enable trace logging to see full source)"),
      lA.trace(A),
      t.startsWith("codeFile/") ||
        (lA.reportCriticalError(new Error(l, { cause: k }), { moduleName: t, moduleSource: A }),
        L("application_error", { message: l, area: "compiler" })),
      k7(k)
    );
  } finally {
    (s?.end(), Me(`\u{1F4DD} Compile ${t}`, i, void 0, "vekter"));
  }
}
async function d7(t) {
  let A = performance.now();
  try {
    return { ok: !0, value: await (await tj()).analyzeImports(t) };
  } catch (j) {
    return (
      lA.warn("Failed to analyze imports:", j),
      j instanceof Error ? { ok: !1, error: j } : { ok: !1, error: new Error(`${j}`, { cause: j }) }
    );
  } finally {
    let j = performance.now();
    Me("\u{1F575}\u{1F3FB} Analyze Imports", A, j, "vekter");
    let e = j - A;
    lA.debug("\u{1F575}\u{1F3FB} Analyze Imports took", e.toFixed(2), "ms");
  }
}
var Kj;
async function tj() {
  if (!Kj) {
    let t = new Worker(Et("./modulesCompilerWorker.js")),
      A = dt(t);
    ((Kj = A.initWithPathToWasm(a7).then(() => ({
      transformSync: A.transformSync,
      analyzeImports: A.analyzeImports,
    }))),
      Kj.catch(() => {
        if (ur() || !c6.isProduction)
          throw new Error(`\u274C Your dev setup is struggling to run the compiler\u2019s WASM binary.
To fix it, run "make clean" followed by "make dev".`);
      }));
  }
  return Kj;
}
var cA = l6(m6(), 1),
  E7 = "dismissedInstantNPMToast",
  B7 = "showedInstantNPMToast",
  p7 = "instant-npm-toast",
  Si = () => window.localStorage.getItem(B7) === "true",
  u7 = () => window.localStorage.getItem(E7) === "true";
function ho() {
  return (0, cA.jsxs)(cA.Fragment, {
    children: [
      (0, cA.jsx)(Oe, { children: "Installed NPM library." }),
      (0, cA.jsxs)("a", {
        href: "https://www.framer.com/developers/#faq",
        target: "_blank",
        children: [" ", (0, cA.jsx)(Oe, { children: "Learn more." })],
      }),
    ],
  });
}
function G7() {
  (window.localStorage.setItem(B7, "true"),
    X({
      type: "add",
      variant: "info",
      text: (0, cA.jsx)(ho, {}),
      key: p7,
      duration: 1 / 0,
      showCloseButton: "never",
      action: { title: "Dismiss", onClick: () => window.localStorage.setItem(E7, "true") },
    }));
}
function yi() {
  X({ type: "remove", key: p7 });
}
function Li(t, A, j, e, { shouldOpenPage: r = !0, renamedIds: o = new SA() } = {}) {
  let i = Rr(t, A, j, "duplicate", {
    enterIsolation: !1,
    preferredName: e,
    insertionIndex: t.tree.root.children.findIndex((n) => n.id === A.id) + 1,
    renamedIds: o,
  });
  return (
    t.stores.persistedUserDefaults.newContentAsDraft &&
      i.set({ isDraft: !0, duplicatedFrom: Q6(i.duplicatedFrom, A.id) }),
    r && t.stores.scopeStore.select(i.id, { keepHistory: !1 }),
    i
  );
}
function _o(t, A, j) {
  let e = new SA(),
    r = new Set(
      t.stores.scopeStore
        .getDesignPageNodes()
        .map((s) => s.resolveValue("name"))
        .filter(m)
    ),
    o = KA(j ?? A.resolveValue("name") ?? "Design", r),
    i = A.clone({ name: o, children: new q6() });
  e.set(A.id, i.id);
  let n = [];
  for (let s of A.children) {
    let a = Gr(s, new Map(), new Map(), e, !1);
    (n.push(a), e.set(s.id, a.id), i.addChild(a));
  }
  t.tree.insertNode(i, void 0, t.tree.root.children.findIndex((s) => s.id === A.id) + 1);
  for (let s of n) pr(t.tree, t.componentLoader, s, e, !1);
  return (
    ar(t.tree, A.id, i.id),
    L("design_page_create", { pageId: i.id, source: "duplicate" }),
    i
  );
}
async function wi(t, A, j) {
  let e = A.isLoaded() ? A : await A.load();
  if (e) return _o(t, e, j);
}
function Qi(t, A, j) {
  let e = A && t.getNode(A);
  if (!e || !iA(e)) return j(A, {}, !1);
  let r = Z6(t, e) ? {} : { visible: !1 },
    o = j(e.originalid, r, !0),
    i = NA.getReplicaForTemplateNode(t, e);
  return i ? (r.visible === !1 && At(t, i, o, { visible: !0 }), X6(i.id, o)) : o;
}
function R7(t, A) {
  let j = t.children;
  return I6(t) ? (!j || j.length === 0 ? [] : (A.getChildrenRects(t.id) ?? [])) : null;
}
function I7(t, A, j) {
  if ((t >= A && t <= j) || (t <= A && t >= j)) return 0;
  let e = Math.abs(A - t),
    r = Math.abs(j - t);
  return Math.min(e, r);
}
function bo(t, A, j) {
  let e = t ? "y" : "x",
    r = t ? "height" : "width",
    o = A[e],
    i = 0,
    n = 1 / 0;
  return (
    j.forEach((s, a) => {
      if (n === 0) return;
      let k = s[e],
        l = k + s[r],
        c = I7(o, k, l);
      c < n && ((i = a), (n = c));
    }),
    i
  );
}
function So(t, A, j, e) {
  let r = Math.max(t, j),
    o = Math.min(A, e);
  return r < o;
}
function yo(t, A, j) {
  let e = t ? "y" : "x",
    r = t ? "height" : "width",
    o = j[A];
  d(o, () => `Out of bound access to childRects: ${A} ${JSON.stringify(j)}`);
  let i = o[e],
    n = i + o[r],
    s = new Set([A]);
  return (
    j.forEach((a, k) => {
      if (k === A) return;
      let l = a[e],
        c = l + a[r];
      So(i, n, l, c) && s.add(k);
    }),
    { startIndex: Math.min(...s), endIndex: Math.max(...s) }
  );
}
function Oo(t, A, j, e, r, o) {
  if (r) return e.startIndex;
  if (o) return e.endIndex;
  let i = A ? "x" : "y",
    n = A ? "width" : "height",
    s = t[i],
    a = 1 / 0,
    k = e.startIndex;
  for (let l = e.startIndex; l <= e.endIndex; l++) {
    let c = j[l];
    d(c, () => `Ouf of bound access to childRects: ${l} ${JSON.stringify(e)} ${JSON.stringify(j)}`);
    let $ = c[i],
      B = $ + c[n],
      E = I7(s, $, B);
    E < a && ((a = E), (k = l));
  }
  return k;
}
function Mo(t, A) {
  let j = [];
  for (let e = A.startIndex; e <= A.endIndex; e++) {
    let r = t[e];
    (d(
      r,
      () => `Ouf of bound access to childRects: ${e} ${JSON.stringify(A)} ${JSON.stringify(t)}`
    ),
      j.push(r));
  }
  return nA.merge(...j);
}
function vo(t, A, j, e) {
  let r = se(t) || t.resolveValue("stackDirection") === "horizontal",
    o = nA.merge(...A),
    i = r ? "x" : "y",
    n = r ? "y" : "x",
    s = r ? "width" : "height",
    a = r ? "height" : "width",
    k = j[i],
    l = j[n],
    c = o[n],
    $ = c + o[a],
    B = l < c,
    E = !B && l > $,
    p = bo(r, j, A),
    R = yo(r, p, A),
    g = Oo(j, r, A, R, B, E),
    F = A[g];
  d(F, () => `Closest rect is not defined: ${g}`);
  let h = F[i] + F[s] / 2,
    _ = B || E ? B : k < h,
    b = _ ? g : g + 1;
  (B && (b = 0), E && (b = A.length));
  let T = F[i],
    x = T + F[s],
    f = _ ? T : x,
    S = null,
    y = _ ? g - 1 : g + 1;
  if (y >= R.startIndex && y <= R.endIndex) {
    let $A = A[y];
    if ($A) {
      let dA = $A[i],
        je = dA + $A[s];
      S = _ ? je : dA;
    }
  }
  let I = f;
  W(S) && (I = Math.round((I + S) / 2));
  let Z = Mo(A, R),
    FA = Math.round(Z[n] + Z[a] / 2),
    fA = { [i]: I, [n]: FA },
    Q = { ...fA },
    vA = { ...fA },
    lj = e[a],
    IA = Math.round(lj / 2);
  return (
    r ? ((Q.y -= IA), (vA.y += IA)) : ((Q.x -= IA), (vA.x += IA)),
    { insertionIndex: b, localInsertionLine: { a: Q, b: vA } }
  );
}
function Po(t, A) {
  let j = { insertionIndex: 0, localInsertionLine: { a: { ...A }, b: { ...A } } };
  if (t.length === 0) return j;
  let e = new Map(),
    r = 1e3;
  t.forEach((c, $) => {
    let B = Math.round(c.x * r),
      E = e.get(B);
    E
      ? (E.indices.push($), c.width > E.width && (E.width = c.width))
      : e.set(B, { x: c.x, width: c.width, indices: [$] });
  });
  let o = Array.from(e.values()).sort((c, $) => c.x - $.x),
    i = o.find((c) => A.x >= c.x && A.x <= c.x + c.width);
  if (
    (i ||
      (i = o.reduce((c, $) => {
        if (!c) return $;
        let B = $.x + $.width / 2,
          E = c.x + c.width / 2;
        return Math.abs(A.x - B) < Math.abs(A.x - E) ? $ : c;
      }, o[0])),
    !i)
  )
    return j;
  let n = [...i.indices].sort((c, $) => {
      let B = t[c],
        E = t[$];
      return !B || !E ? 0 : B.y - E.y;
    }),
    s = n.findIndex((c) => {
      let $ = t[c];
      return $ ? A.y < $.y + $.height / 2 : !1;
    });
  s === -1 && (s = n.length);
  let a;
  s < n.length ? (a = n[s] ?? 0) : (a = (n[n.length - 1] ?? 0) + 1);
  let k;
  if (s <= 0) {
    if (!n[0]) return j;
    k = t[n[0]]?.y ?? 0;
  } else if (s >= n.length) {
    let c = n[n.length - 1];
    if (!c) return j;
    let $ = t[c];
    if (!$) return j;
    k = $.y + $.height;
  } else {
    let c = n[s];
    if (!c) return j;
    let $ = t[c];
    if (!$) return j;
    k = $.y;
  }
  let l = { a: { x: i.x, y: k }, b: { x: i.x + i.width, y: k } };
  return { insertionIndex: a, localInsertionLine: l };
}
var F7 = 4,
  f7 = 20;
function g7(t, A, j, e, r) {
  if (
    (d(A.length, "childInsertion only works correctly if there are more than 0 child rects"),
    D.isOn("betterStackGridItemMoving"))
  ) {
    let { rect: l, index: c } = nA.closestRect(A, e),
      $ = ie(t),
      B = g6(t) ? !$ : t.resolveValue("stackDirection") === "horizontal",
      E = B ? "x" : "y",
      p = B ? "y" : "x",
      R = B ? "width" : "height",
      g = B ? "height" : "width",
      F = e[E] < l[E] + l[R] / 2,
      h = $ && W(t.gridColumnCount) ? Math.max(Math.floor(t.gridColumnCount), 1) : 1,
      _ = F ? c - h : c + h,
      b = A[_],
      T = !b || (F ? b[E] >= l[E] : l[E] >= b[E]),
      x = F ? c : Math.min(c + h, A.length);
    if (T) {
      let f = F ? l[E] + F7 : l[E] + l[R] - F7;
      return {
        insertionIndex: x,
        localInsertionLine: { a: Lj(E, f, l[p]), b: Lj(E, f, l[p] + Math.max(l[g], f7)) },
      };
    } else {
      let f = F ? (b[E] + b[R] + l[E]) / 2 : (l[E] + l[R] + b[E]) / 2;
      return {
        insertionIndex: x,
        localInsertionLine: {
          a: Lj(E, f, Math.min(b[p], l[p])),
          b: Lj(E, f, Math.max(b[p] + b[g], l[p] + Math.max(l[g], f7))),
        },
      };
    }
  }
  if (se(t) || t.stackWrapEnabled) return ie(t) ? Po(A, e) : vo(t, A, e, r);
  let o = 1 / 0,
    i = null,
    n = t.stackDirection === "vertical";
  A.forEach((l, c) => {
    let $ = Math.abs(n ? e.y - l.y : e.x - l.x);
    $ > o || ((o = $), (i = c));
  });
  let s = 0;
  if (i !== null) {
    let l = A[i];
    d(l, () => `Closest child rect is not defined: ${i}`);
    let c = nA.center(l);
    ((n ? e.y > c.y : e.x > c.x) && i++, (s = Math.min(Math.max(0, i), A.length)));
  }
  let a = Co(t),
    k = Do(t.resolveValue("stackDirection"), t.resolveValue("stackAlignment"), a, j, A, s, r);
  return { insertionIndex: To(t, s), localInsertionLine: k };
}
function Lj(t, A, j) {
  return t === "x" ? { x: A, y: j } : { x: j, y: A };
}
function To(t, A) {
  d(YA(t), "A node must support children to consider an insertion index.");
  let j = A;
  for (let e = 0; e < t.children.length && e !== A; e++) {
    let r = t.children.at(e);
    r && (rt(r) || j++);
  }
  return j;
}
function Co(t) {
  let A = t.resolveValue("stackDirection"),
    j = t.resolveValue("stackAlignment");
  if (j === "center") return 0;
  let e = tt(t);
  if (!e.perSide) return e.top;
  let r = j === "start";
  return A === "vertical" ? (r ? e.left : e.right) : r ? e.top : e.bottom;
}
function Do(t, A, j, e, r, o, i) {
  let n = r[o - 1],
    s = r[o],
    a = t === "vertical",
    k = { x: e.width / 2, y: e.height / 2 };
  if (!(!n && !s)) {
    if (r.length === 1) {
      let l = n || s;
      (d(l, "Child rect must be defined"),
        o === 0
          ? a
            ? (k.y = l.y - 10)
            : (k.x = l.x - 10)
          : a
            ? (k.y = l.y + l.height + 10)
            : (k.x = l.x + l.width + 10));
    } else if (n && s) {
      let l = Math.abs(a ? n.y + n.height - s.y : n.x + n.width - s.x);
      a ? (k.y = s.y - l / 2) : (k.x = s.x - l / 2);
    } else if (n) {
      let l = r[o - 2];
      d(l, "Child rect must be defined");
      let c = Math.abs(a ? l.y + l.height - n.y : l.x + l.width - n.x);
      a ? (k.y = n.y + n.height + c / 2) : (k.x = n.x + n.width + c / 2);
    } else if (s) {
      let l = r[o + 1];
      d(l, "Child rect must be defined");
      let c = Math.abs(a ? s.y + s.height - l.y : s.x + s.width - l.x);
      a ? (k.y = s.y - c / 2) : (k.x = s.x - c / 2);
    }
  }
  return (
    (k.x = Math.min(Math.max(4, k.x), e.width - 4)),
    (k.y = Math.min(Math.max(4, k.y), e.height - 4)),
    xo(t, A, j, k, e, i)
  );
}
function xo(t, A, j, e, r, o) {
  let i = t === "vertical",
    n,
    s,
    a = j ?? 0;
  switch (A) {
    case "center":
      ((n = { x: i ? e.x - o.width / 2 : e.x, y: i ? e.y : e.y - o.height / 2 }),
        (s = { x: i ? e.x + o.width / 2 : e.x, y: i ? e.y : e.y + o.height / 2 }));
      break;
    case "start":
      ((n = { x: i ? a : e.x, y: i ? e.y : a }),
        (s = { x: i ? o.width + a : e.x, y: i ? e.y : o.height + a }));
      break;
    default:
      ((n = { x: i ? r.width - o.width - a : e.x, y: i ? e.y : r.height - o.height - a }),
        (s = { x: i ? r.width - a : e.x, y: i ? e.y : r.height - a }));
      break;
  }
  return { a: n, b: s };
}
var h7 = 10;
function No(t, A) {
  for (let j of t) if (A.includes(j)) return !0;
  return !1;
}
function qo(t, A, j, e, r) {
  if (r.length === 0 || t.stackWrapEnabled || pA(t) || iA(t)) return null;
  let { tree: o } = A,
    { zoom: i } = A.stores.canvasStore,
    n = o.getParent(t.id);
  if (n && CA(n) && (n.stackDirection !== t.stackDirection || n.stackWrapEnabled)) return null;
  let s = o.convertPointToNode(t, j),
    a = o.getRect(t),
    k = t.stackDirection === "vertical",
    l = k ? "x" : "y",
    c = k ? "y" : "x",
    $ = k ? "width" : "height",
    B = k ? "height" : "width",
    E = a[$],
    p = a[B],
    R = E * i,
    g = p * i;
  if (R < 40 || g < 40) return null;
  let F = s[l],
    h = 10 / i,
    _ = nA.merge(...r),
    b = _[l],
    T = b + _[$],
    x = Math.max(h, b / 2),
    f = Math.max(h, (E - T) / 2),
    S = F <= x,
    y = F >= E - f;
  if (!S && !y) return null;
  let I = S ? 0 : 1,
    Z = e[B],
    FA = 4 / i,
    fA = S ? FA : E - FA,
    Q = p / 2,
    vA = Z / 2,
    lj = p / 3,
    IA = s[c],
    PA = "end";
  Z > p ? (PA = "center") : IA < lj ? (PA = "start") : IA < 2 * lj && (PA = "center");
  let $A, dA;
  switch (PA) {
    case "start": {
      (($A = 0), (dA = Z));
      break;
    }
    case "end": {
      ((dA = p), ($A = dA - Z));
      break;
    }
    default: {
      (($A = Q - vA), (dA = Q + vA));
      break;
    }
  }
  let je = { [l]: fA, [c]: $A },
    mo = { [l]: fA, [c]: dA },
    Ro = o.convertPointToCanvas(t, je),
    Fo = o.convertPointToCanvas(t, mo);
  return { insertionIndex: I, insertionLine: { a: Ro, b: Fo }, wrap: { wrapAlignment: PA } };
}
function Ko(t, A, j, e, { allowWrapping: r = !0 } = {}) {
  if (!YA(t)) return { insertionIndex: null, insertionLine: null };
  let o = R7(t, A.stores.layoutCache);
  if (!o) return { insertionIndex: null, insertionLine: null };
  if (CA(t) && r) {
    let c = qo(t, A, j, e, o);
    if (c) return c;
  }
  let i = A.tree,
    n = i.getRect(t),
    s = i.convertPointToNode(t, j);
  if (o.length === 0) return { insertionIndex: 0, insertionLine: null };
  let { insertionIndex: a, localInsertionLine: k } = g7(t, o, n, s, e),
    l = { a: i.convertPointToCanvas(t, k.a), b: i.convertPointToCanvas(t, k.b) };
  return { insertionIndex: a, insertionLine: l };
}
var Lo = { width: 10, height: 10 };
function Ia(t, A, j, e, r = !1) {
  d(j.length, "getStackOrGridInRange(): expects at least a single selected node");
  let o = t.stores.scopeStore.active;
  if (j.some(W6)) return null;
  let n = t.tree
    .getNodesAtPoint(o, A)
    .filter(
      (k) =>
        !(
          (it(k) && k.locked) ||
          !YA(k) ||
          yr(k) ||
          Y6(t.tree, t.stores.overlayStore.activeOverlays, k)
        )
    )
    .reverse();
  if (e) {
    let k = e.id,
      l = n.find((c) => c.id === k);
    if (l) return l;
  }
  let s = null,
    a = j.map((k) => k.id);
  for (let k of n) {
    if (!K6(k) || (r && !N6(k))) continue;
    let l = t.tree.getAncestors(k.id);
    if ((l.push(k.id), No(l, a) || (s || (s = k), !ke(k)))) continue;
    let { insertionLine: $ } = Ko(k, t, A, Lo);
    if (!$) continue;
    if ($.a.x === $.b.x) {
      if (Math.abs(A.x - $.a.x) * t.stores.canvasStore.zoom > h7) continue;
    } else if ($.a.y === $.b.y) {
      if (Math.abs(A.y - $.a.y) * t.stores.canvasStore.zoom > h7) continue;
    } else continue;
    if (j.every((E) => Se(t.tree, k, E, o.id, t.componentLoader))) return k;
  }
  if (ke(s)) {
    let k = s;
    if (j.every((c) => Se(t.tree, k, c, o.id, t.componentLoader))) return s;
  }
  return null;
}
function ga(t, A, { wrapAlignment: j }) {
  let e = Pj.addStack(t, [A]);
  if (!e) return null;
  let r = t.tree.getNode(e);
  return !r || !CA(r)
    ? null
    : (r.set({
        stackDirection: A.stackDirection === "horizontal" ? "vertical" : "horizontal",
        stackAlignment: j,
      }),
      r);
}
function _7(t, A, j = !0) {
  t.scheduler.processWhenReady(() => {
    let {
      overlayStore: e,
      selectionStore: r,
      scopeStore: o,
      canvasStore: i,
      persistedUserDefaults: n,
    } = t.stores;
    o.selectByNode(A.id);
    let s = WA(A) ? A : t.tree.getNodeWithTrait(A.cache.overlayAncestorId, WA);
    if ((s ? e.showOverlay(r, s, s.parentid) : e.hideAll(), r.set(A.id), j)) {
      let a = nt(t.tree, [A]);
      i.zoomToCenter(a, { animated: n.animateOnZoom, maxZoom: 1 });
    }
  });
}
var rj = BA("unlinking");
async function wo(t, A, j) {
  let { codeEditorStore: e, selectionStore: r, treeStore: o } = t.stores;
  e.closeEditor();
  let [i] = r.nodes;
  if (r.nodes.length === 1 && v(i) && i.codeComponentIdentifier === A) return !0;
  let n;
  if (o.getDataTree()) {
    for (let s of o.query().whereClass(jj).iterate())
      if (s.codeComponentIdentifier === A) {
        n = s;
        break;
      }
  } else
    n = await t.runWithFullyLoadedTreeAsync(
      () => {
        for (let s of o.treeIndex.codeComponentNodeIds) {
          let a = t.tree.getNodeWithTrait(s, v);
          if (a?.codeComponentIdentifier === A) return a;
        }
      },
      { runInBackground: !0, name: `findCodeComponentInstance: ${A}` }
    );
  return n
    ? (_7(t, n), !0)
    : (X({
        type: "add",
        variant: "info",
        key: "add-component-instance-to-edit-in-plugin",
        primaryText: "Add component to Canvas",
        secondaryText: `to edit in ${j.name}.`,
      }),
      !1);
}
async function wj(t, A, j) {
  return (await wo(t, A, j)) ? (t.stores.pluginStore.openPlugin(j, t, { mode: "canvas" }), !0) : !1;
}
function Jj(t, A) {
  let j = t.componentForIdentifier(A);
  if (j) return j;
  let e = G(A);
  return !K(e) || !J(e) ? null : t.componentForIdentifier($6(e));
}
var Jo = 0;
async function Hj(t, A, j = Jo) {
  if (j === 0) {
    let r = G(A),
      o = vj(t.stores.treeStore, t.stores.modulesStore, t.stores.pluginStore, r);
    if (o && (await wj(t, A, o))) return;
  }
  let e = Jj(t.componentLoader, A);
  e && t.stores.codeEditorStore.editFile(e.file);
}
async function b7(
  t,
  {
    identifier: A,
    info: j,
    instance: e,
    enterIsolation: r,
    codeComponentIsolationMode: o,
    scheduleTreeUpdate: i = (n) => t.scheduler.processWhenReadyAsync(n),
  }
) {
  let n = j.module.baseURL + j.module.files.source,
    a = await (await fetch(n)).text(),
    [k, l] = await Promise.all([d7(a), t.stores.modulesStore.getModuleDependencies(A)]);
  if (k.ok) {
    let E = new Map();
    for (let R of k.value.relative) {
      let g = Ij(R.specifier, `${j.module.type}/`);
      if (!g) {
        rj.warn("Failed to normalize relative import", R.specifier);
        continue;
      }
      let F = E.get(g) ?? [];
      (F.push(R), E.set(g, F));
    }
    let p;
    for (let R of l.imports) {
      if (R.type !== "local") continue;
      let g = `${R.moduleType}/${R.moduleName}`,
        F = E.get(g);
      if (!F) {
        rj.warn("Unable to get source location of import", g, "when unlinking");
        continue;
      }
      for (let h of F) {
        p ||
          (p = a.split(`
`));
        let _ = h.specifierLine;
        p[_] = p[_].replace(JSON.stringify(h.specifier), JSON.stringify(R.importURL));
      }
    }
    p &&
      (a = p.join(`
`));
  }
  let $ = await t.stores.modulesStore
      .forType("codeFile")
      .createWithUniqueName({
        name: j.module.name,
        source: a,
        metadata: { pluginId: j.module.metadata.pluginId },
      }),
    B = E6(A, $).value;
  return (
    e &&
      (await i(() => {
        let E = t.tree.get(e.getPrimaryId());
        E && E.set({ codeComponentIdentifier: B });
      })),
    r && ((t.stores.chromeStore.contentPanelTab = "Assets"), Hj(t, B, o)),
    B
  );
}
var y7 = { canvasComponent: Fr, codeFile: b7 };
function Je(t) {
  return m(t) ? t in y7 : !1;
}
async function He(
  t,
  {
    identifier: A,
    moduleType: j,
    instance: e,
    enterIsolation: r = !0,
    codeComponentIsolationMode: o,
    renamedIds: i = new SA(),
    isAutoDetach: n = !1,
    scheduleTreeUpdate: s,
  }
) {
  if (!Je(j)) throw Error(`Cannot import external module of type ${j}`);
  let [a] = await t.stores.modulesStore.preloadExternalModules([A]);
  d(a, "external module must exist on preload");
  let k = {
      identifier: A,
      info: a,
      instance: e,
      enterIsolation: r,
      codeComponentIsolationMode: o,
      renamedIds: i,
      isAutoDetach: n,
      scheduleTreeUpdate: s,
    },
    l = y7[j],
    c = await l(t, k);
  if (!_A(c))
    return !m(c) && jA(c)
      ? { ...a, codeComponentIdentifier: c.instanceIdentifier, node: c }
      : { ...a, codeComponentIdentifier: c, node: void 0 };
}
var we = new Set();
async function Ve(t, A) {
  let { instance: j, withUnlinkModal: e, ...r } = A,
    o = xj(t.stores.treeStore.tree, A.identifier),
    i = t.stores.insertSidebarStore.getEditBehavior(A.identifier.moduleId);
  if (e && i !== "autoUnlink" && (!o || Tr.isOn("openPrimaryForBuiltInModules"))) {
    t.stores.modalStore.set({
      type: "UnlinkComponent",
      source: "edit_action",
      ...r,
      nodeId: j?.id,
    });
    return;
  }
  if (we.has(A.identifier)) return;
  we.add(A.identifier);
  let n = A.skipUndoGroup !== !0;
  n &&
    t.scheduler.process(() => {
      t.beginUndoGroup();
    });
  try {
    return (await He(t, { instance: j, enterIsolation: i !== "autoDetach", ...r }))
      ?.codeComponentIdentifier;
  } catch (s) {
    (rj.reportError(s),
      X({
        type: "add",
        variant: "error",
        primaryText: "Failed to unlink",
        secondaryText: "component.",
        key: "external-component-unlink-error",
        icon: "error",
      }));
  } finally {
    (n &&
      t.scheduler.processWhenReady(() => {
        t.endUndoGroup();
      }),
      we.delete(A.identifier));
  }
}
async function O7(t, A) {
  let { instance: j, moduleType: e, enterIsolation: r, codeComponentIsolationMode: o } = A,
    i = A.scheduleTreeUpdate ?? ((k) => t.scheduler.processWhenReadyAsync(k));
  if (!v(j)) return;
  let n = await Ve(t, { ...A, enterIsolation: !1 });
  if (!n) return;
  await dr(t.componentLoader, [n], t.stores.modulesStore, "component-unlinking");
  let s = t.componentLoader.componentForIdentifier(n);
  if (!s) return;
  let a = vr(s);
  a &&
    (await i(() => {
      if (
        (t.tree
          .getNodes(Array.from(t.stores.treeStore.treeIndex.codeComponentNodeIds))
          .forEach((l) => {
            if (
              !a ||
              a.id === l.id ||
              !v(l) ||
              l.codeComponentIdentifier !== j.codeComponentIdentifier ||
              iA(l) ||
              !Or(l, { allowLockedNodes: !0 })
            )
              return;
            let c = t.cloneNode(a, !1, !1);
            Mr(t, [l], c);
          }),
        !!r)
      )
        if (e === "canvasComponent") {
          let l = Nt(n);
          l && t.stores.scopeStore.select(l, { keepHistory: !1 });
        } else e === "codeFile" && Hj(t, n, o);
    }));
}
function M7(t, A, j) {
  if (Mj(t, "canDesign")) return;
  let r = t.tree.getNodeWithTrait(A, U)?.annotation("framerVector");
  if (!qA(r)) return;
  let o = r.set.moduleId,
    n = t.tree.getNodeWithTrait(o, U)?.annotation("framerVectorSet");
  Aj(n) &&
    t.stores.modalStore.set({
      type: "EditVector",
      source: "edit_action",
      vectorSetNodeId: o,
      moduleId: A,
      itemCount: n.items.length,
      instanceNodeId: j,
    });
}
function Ue(t) {
  return t.type === "existing" || t.type === "privateDraft";
}
function bs({ stackElement: t, tree: A }) {
  if (!t) return null;
  let j = A.getNodeWithTrait(t.collectionId, Y);
  if (j) return j;
  let e = t.type === "arrayItem" ? t.collectionItemId : t.itemId,
    r = A.getNodeWithTrait(e, P);
  return r?.parentid ? A.getNodeWithTrait(r.parentid, Y) : null;
}
function zo({ treeStore: t, stackElement: A }) {
  if (!A) return null;
  if (A.type === "existing" || A.type === "privateDraft")
    return t.tree.getNodeWithTrait(A.itemId, P);
  if (A.type === "arrayItem") return t.tree.getNodeWithTrait(A.collectionItemId, P) ?? null;
  N(A);
}
function Ss({ treeStore: t, stackElement: A }) {
  return !A || !Xe(A) ? null : zo({ treeStore: t, stackElement: A });
}
function ys(t) {
  return t.findLast(Ue) ?? null;
}
function Os(t) {
  return t.reduce((A, j) => (Ue(j) ? A + 1 : A), 0);
}
function Ms(t, A = []) {
  let j = t.stores.selectionStore.ids.length === 1 ? t.stores.selectionStore.ids.at(0) : void 0,
    e = Wo(t, A),
    r = e.at(0),
    o = t.tree.getNodeWithTrait(j, P),
    i = o && Xj(t, o) ? o : null;
  if (i) {
    if (!r) return [v7(t, i)];
    if (r.type === "existing")
      return r.itemId === i.id
        ? e
        : (d(e.length === 1, "Must dismiss overlay stack before changing selection"), [v7(t, i)]);
    if (r.type === "privateDraft") {
      if (r.itemId === i.id) return e;
      throw new Error("Must save private draft item stack before selecting a collection item");
    }
    if (r.type === "arrayItem") return e;
    N(r);
  }
  if (!r || r.type === "privateDraft") return e;
  if (r.type === "existing")
    return t.stores.chromeStore.mainView !== 2
      ? e
      : (d(e.length === 1, "Must dismiss overlay stack before de-selecting collection item"), []);
  if (r.type === "arrayItem") return e;
  N(r);
}
function Wo(t, A) {
  let j = [];
  for (let e = 0; e < A.length; e++) {
    let r = A[e];
    if (!r) continue;
    if (r.type === "existing") {
      let n = t.tree.getNodeWithTrait(r.itemId, P);
      n && Uj(t, n)
        ? (r = Vj(t, n, { saveAction: r.saveAction, showErrors: !1 }))
        : n?.parentid &&
          r.collectionId !== n.parentid &&
          (r = Vj(t, n, { saveAction: r.saveAction, showErrors: !1 }));
    }
    if (r.type === "privateDraft") {
      let n = t.tree.getNodeWithTrait(r.itemId, P);
      n && Xj(t, n) && !Uj(t, n) && (r = Vj(t, n, { saveAction: r.saveAction, showErrors: !1 }));
    }
    let o = j.at(-1) ?? null;
    if (!Yo(t, r, o) || (e > 0 && Xe(r) && !r.saveAction)) break;
    j.push(r);
  }
  return j;
}
function Yo(t, A, j) {
  if (A.type === "existing") {
    let e = t.tree.getNodeWithTrait(A.itemId, P);
    return e ? !!e.parentid && Xj(t, e) : !1;
  }
  if (A.type === "privateDraft") {
    let e = t.tree.getNodeWithTrait(A.itemId, P);
    return e ? e.parentid === A.collectionId && Uj(t, e) : !1;
  }
  if (A.type === "arrayItem") {
    if (
      !j ||
      !Ue(j) ||
      j.itemId !== A.collectionItemId ||
      (Xe(j) && j.collectionId !== A.collectionId)
    )
      return !1;
    let e = t.tree.getNodeWithTrait(A.collectionId, Y);
    if (!e) return !1;
    let r = t.tree.getNodeWithTrait(A.collectionItemId, P);
    if (!r || !Xj(t, r) || (r.parentid && r.parentid !== A.collectionId)) return !1;
    let o = e.getVariable(A.arrayFieldId);
    if (!o || !vt(o)) return !1;
    let i = r.getControlProp(A.arrayFieldId);
    return !i || !st(i) || !VA(i.value)
      ? !1
      : i.value.some(({ type: n, id: s }) => n === "object" && s === A.arrayItemId);
  }
  N(A);
}
function Uj(t, A) {
  return kr(A, { currentUserId: t.stores.sessionStore.user.id });
}
function Xj(t, A) {
  return lr(A, { currentUserId: t.stores.sessionStore.user.id });
}
function Xe(t) {
  return t.type === "privateDraft";
}
function v7(t, A) {
  return Vj(t, A, { showErrors: !1 });
}
function Vj(t, A, j) {
  if (A.parentid && Uj(t, A)) {
    let r = {
      collectionId: A.parentid,
      itemId: A.id,
      showErrors: j.showErrors,
      type: "privateDraft",
    };
    return (
      j.saveAction && (r.saveAction = j.saveAction),
      j.attemptedClose && (r.attemptedClose = j.attemptedClose),
      r
    );
  }
  d(A.parentid, "Existing collection item must have a parent collection");
  let e = { collectionId: A.parentid, itemId: A.id, type: "existing" };
  return (j.saveAction && (e.saveAction = j.saveAction), e);
}
function C7(t) {
  return t.type === "controlReference";
}
function Qe(t, A, j) {
  let e = t.get(A);
  if (e) return e;
  let r = j();
  return (t.set(A, r), r);
}
function D7() {
  return {};
}
function ze(t) {
  let A = new Map();
  for (let j of t) {
    let e = G(j.identifier);
    if (!K(e) || !J(e) || e.type !== "codeFile") continue;
    let r = Qe(A, e.localId, D7);
    r[e.exportSpecifier] = Ge(j.properties);
  }
  return A;
}
function We(t, A) {
  let j = new Map();
  for (let e of t) {
    let r = G(e.identifier);
    if (!q(r) || !J(r) || (A && !A.has(r.moduleId))) continue;
    let o = Qe(j, r.moduleId, () => new Map()),
      i = Qe(o, r.saveId, D7);
    i[r.exportSpecifier] = Ge(e.properties);
  }
  return j;
}
function Qj(t) {
  let A = G(t.codeComponentIdentifier);
  if (q(A)) return A.saveId;
}
function Ye(t, A, j) {
  return t.get(A)?.get(j);
}
function Ze(t, A, j) {
  return { ...t?.propertyControlsBySaveId, [A]: Nj(j) };
}
function x7(t) {
  for (let A of kA.getModuleNodes(t)) {
    let j = Qj(A);
    if (j && A.propertyControlsBySaveId?.[j] === void 0) return !0;
  }
  return !1;
}
function N7(t) {
  let A = [];
  for (let j of kA.getModuleNodes(t)) {
    let e = Qj(j);
    if (!e || j.propertyControlsBySaveId?.[e] !== void 0) continue;
    let r = G(j.codeComponentIdentifier);
    q(r) && A.push(r);
  }
  return A;
}
function P7(t, A, j, e, r) {
  if (!t) return;
  let o = A ?? jn(e, t);
  if (!o) return;
  let i = e.getObjectKey(t, "propertyControlsBySaveId");
  return q7(i?.[o], j, r);
}
function Zo(t, A) {
  let j = A.parsedPropertyControls.get(t);
  if (j !== void 0) return j ?? void 0;
  let e = fr(t),
    r = An(e) ? e : null;
  return (A.parsedPropertyControls.set(t, r), r ?? void 0);
}
function An(t) {
  if (!AA(t)) return !1;
  for (let A in t) {
    let j = t[A];
    if (!AA(j)) return !1;
    for (let e in j) if (!AA(j[e])) return !1;
  }
  return !0;
}
function q7(t, A, j) {
  if (t !== void 0) return t === null ? null : Zo(t, j)?.[A];
}
function jn(t, A) {
  let j = t.getObjectKey(A, "codeComponentIdentifier");
  if (!m(j)) return;
  let e = G(j);
  if (q(e)) return e.saveId;
}
function en(t, A) {
  if (!(!t || !J(t)))
    switch (t.kind) {
      case "localModuleExport":
        return A.getObjectKey(`${t.localId}.save`, "moduleId");
      case "externalModuleExport":
        return t.moduleId;
      default:
        N(t);
    }
}
function tn(t, A, j) {
  let e = rn(A, t.entityIdentifier),
    r = G(e);
  if (!(!r || !J(r)))
    return {
      type: "controlReference",
      controlKey: t.controlKey,
      title: t.name || "Unknown",
      description: t.description,
      defaultValue: t.initialValue,
      optional: _e(t, j) ? !0 : void 0,
      __defaultAssetReference:
        t.expectedType === "file" && m(t.initialValue) ? t.initialValue : void 0,
      module: { identifier: r.value, moduleId: en(r, A) },
    };
}
function rn(t, A) {
  let j = G(A);
  if (!q(j)) return A;
  let e = t.getObjectKey(j.moduleId, "codeComponentIdentifier");
  return m(e) ? e : A;
}
function T7(t, A) {
  _r(A) && (t[Nr(A.id)] = { type: "changehandler", changes: A.id });
}
function zj(t, A) {
  let j = t.getObjectKey(A, "variables");
  if (j === void 0 && t.has(A)) return [];
  if (!Array.isArray(j)) return;
  let e = [];
  for (let r of j) bj(r) && e.push(r);
  return e;
}
function on(t, A) {
  let j = zj(A, t);
  if (!j) return;
  let e = Ej("collection", t, "default").value,
    r = oj(j, A, "ContentManagement", !1, {}),
    o = A.getObjectKey(t, "name"),
    i = Dt({ name: m(o) && o ? o : void 0, dataIdentifier: e });
  return oj(i, A, "ContentManagement", !1, r);
}
function K7(t, A, j) {
  let e = A.name || "Unknown",
    r = _e(A, j) ? !0 : void 0;
  switch (A.type) {
    case "array":
      return nn(t, A, j);
    case "boolean":
      return {
        type: A.type,
        title: e,
        description: A.description,
        defaultValue: r ? void 0 : tA(A.type, A.initialValue),
        disabledTitle: A.options?.disabledTitle,
        enabledTitle: A.options?.enabledTitle,
        optional: r,
      };
    case "number":
      return {
        type: A.type,
        title: e,
        description: A.description,
        defaultValue: r ? void 0 : tA(A.type, A.initialValue),
        displayStepper: A.options?.displayStepper,
        max: A.options?.max,
        min: A.options?.min,
        optional: r,
        step: A.options?.step,
        unit: A.options?.unit,
      };
    case "color":
      return { type: A.type, title: e, description: A.description, defaultValue: A.initialValue };
    case "string":
      return {
        type: A.type,
        title: e,
        description: A.description,
        defaultValue: r ? void 0 : tA(A.type, A.initialValue),
        displayTextArea: A.options?.displayTextArea,
        maxLength: A.options?.maxLength,
        optional: r,
        placeholder: A.options?.placeholder,
        preventLocalization: A.preventLocalization,
      };
    case "richtext":
      return {
        type: A.type,
        title: e,
        description: A.description,
        defaultValue: tA(A.type, A.initialValue),
        maxLength: A.options?.maxLength,
        placeholder: A.options?.placeholder,
        preventLocalization: A.preventLocalization,
      };
    case "image":
      return {
        type: "responsiveimage",
        title: e,
        description: A.description,
        __defaultAssetReference: m(A.initialValue) ? A.initialValue : void 0,
      };
    case "eventhandler":
      return { type: A.type, title: e };
    case "link":
    case "scrollsectionref":
    case "customcursor":
    case "trackingid":
      return { type: A.type, title: e, description: A.description };
    case "linkrelvalues":
    case "cursor":
    case "gap":
    case "padding":
    case "borderradius":
    case "location":
      return { type: A.type, title: e, description: A.description, defaultValue: A.initialValue };
    case "date":
      return {
        type: A.type,
        title: e,
        description: A.description,
        defaultValue: r ? void 0 : tA(A.type, A.initialValue),
        displayTime: A.options?.displayTime ? !0 : void 0,
        optional: r,
      };
    case "enum": {
      let o = [],
        i = [];
      for (let n of A.cases) (o.push(n.id), i.push(n.name));
      return {
        type: A.type,
        title: e,
        description: A.description,
        defaultValue: r ? void 0 : tA(A.type, A.initialValue),
        optional: r,
        options: o,
        optionTitles: i,
      };
    }
    case "file": {
      let o = new Set();
      for (let i of A.allowedFileTypes) i.extension && o.add(i.extension);
      return {
        type: A.type,
        title: e,
        description: A.description,
        allowedFileTypes: Array.from(o),
        __defaultAssetReference: tA(A.type, A.initialValue),
      };
    }
    case "transition": {
      let o = tA(A.type, A.initialValue);
      return {
        type: A.type,
        title: e,
        description: A.description,
        defaultValue: o ? D6(o) : void 0,
      };
    }
    case "border": {
      let o = tA(A.type, A.initialValue);
      return {
        type: A.type,
        title: e,
        description: A.description,
        defaultValue: o ? et(o) : void 0,
      };
    }
    case "boxshadow":
      return {
        type: A.type,
        title: e,
        description: A.description,
        defaultValue: A.initialValue.map(bt),
      };
    case "collectionreference":
    case "multicollectionreference":
      return {
        type: A.type,
        title: e,
        dataIdentifier: A.dataIdentifier,
        description: A.description,
        defaultValue: r ? void 0 : A.initialValue,
        optional: r,
      };
    case "vectorsetitem":
      return {
        type: A.type,
        title: e,
        description: A.description,
        defaultValue: A.initialValue,
        setModuleId: A.setModuleId,
      };
    case "slug":
      return { type: "string", title: e, description: A.description, preventLocalization: an(t) };
    case "nodePropertyControlReference":
    case "controlReference":
      return null;
    default:
      N(A);
  }
}
function nn(t, A, j) {
  let e = {},
    r = D.isOn("arrays");
  for (let o of A.itemVariables) {
    if (!bj(o)) continue;
    let i = K7(t, o, j),
      n = i ? gj({ control: i }).control : void 0;
    !n || !G6(n) || (e[o.id] = n);
  }
  return {
    type: A.type,
    title: A.name || "Unknown",
    control: { type: "object", controls: e },
    description: A.description,
    displayGalleryAsArray: Pt(A, r) ? !0 : void 0,
    maxCount: A.maxCount,
    minCount: A.minCount,
  };
}
function an(t) {
  let A = t.getHierarchy().getRootId();
  if (!A) return !1;
  let j = t.getObjectKey(A, "webMetadata");
  return AA(j) ? j.translatePagePaths === !1 : !1;
}
function sn(t, A) {
  let j = A.getObjectKey(t, "baseVariantId");
  if (!m(j) || !A.has(j)) return {};
  let e = [j];
  for (let n of A.getChildrenIds(t))
    n !== j && A.getObjectKey(n, "isVariant") === !0 && (A.getObjectKey(n, "gesture") || e.push(n));
  if (e.length < 2) return {};
  let r = [],
    o = [],
    i = new Set();
  for (let n of e) {
    let s = A.getObjectKey(n, "name"),
      a = (m(s) && s) || KA("Variant", i, { startIndex: i.size + 1, omitCountIfFirst: !1 });
    (i.add(a), r.push(n), o.push(a));
  }
  return { variant: { type: "enum", title: "Variant", options: r, optionTitles: o } };
}
function oj(t, A, j, e, r) {
  for (let o of t) {
    if (!bj(o)) continue;
    let i = C7(o) ? tn(o, A, j) : void 0;
    if (i) {
      ((r[o.id] = i), e && T7(r, o));
      continue;
    }
    let n = K7(A, o, j);
    ne(n) || ((r[o.id] = n), e && T7(r, o));
  }
  return r;
}
function kn(t, A) {
  let j = zj(A, t);
  if (j) return oj(j, A, "SmartComponent", !0, sn(t, A));
}
function ln(t, A) {
  let j = zj(A, t);
  if (j) return oj(j, A, "LayoutTemplate", !1, {});
}
function cn(t, A) {
  let j = A.getParentId(t);
  if (!j || j === Ar || A.getObjectKey(j, "__class") !== "VectorSetNode") return;
  let e = zj(A, j);
  if (e) return oj(e, A, "VectorSet", !1, {});
}
function $n(t, A) {
  let j = G(t.identifier);
  if (!j || !J(j) || !K(j) || j.exportSpecifier !== "default") return;
  let e = j.localIdName;
  switch (j.type) {
    case "canvasComponent":
      return kn(e, A);
    case "layoutTemplate":
      return ln(e, A);
    case "collection":
    case "draftCollection":
      return on(e, A);
    case "vector":
      return cn(e, A);
    case "codeFile":
    case "componentPresets":
    case "config":
    case "prototype":
    case "screen":
    case "css":
    case "webPageMetadata":
    case "siteMetadata":
    case "snippets":
    case "localization":
    case "vectorSet":
    case "design":
    case "kit":
    case "shader":
      return;
    default:
      N(j.type);
  }
}
function dn(t, A, j) {
  let e = G(t.identifier);
  if (!e || !K(e) || !J(e) || e.type !== "codeFile") return;
  let r = A.getObjectKey(`${e.localId}.save`, "propertyControls");
  return q7(r, e.exportSpecifier, j);
}
function En(t, A, j) {
  let e = G(t.identifier);
  if (e)
    switch (e.kind) {
      case "localModuleExport": {
        let r = $n(t, A);
        if (r !== void 0) return r;
        let o = dn(t, A, j);
        return o !== void 0 ? o : P7(t.moduleId, void 0, e.exportSpecifier, A, j);
      }
      case "externalModuleExport":
        return P7(t.moduleId ?? e.moduleId, e.saveId, e.exportSpecifier, A, j);
      default:
        N(e);
    }
}
function Bn(t, A) {
  let j = `${t.module.identifier}\0${t.module.moduleId ?? ""}\0${t.controlKey}`;
  if (A.resolvingReferences.has(j)) return;
  A.resolvingReferences.add(j);
  let r = A.getStoredPropertyControls(t.module, A)?.[t.controlKey];
  if (!r) {
    A.resolvingReferences.delete(j);
    return;
  }
  let o = L7(r, A);
  if ((A.resolvingReferences.delete(j), !!o))
    return gj({
      control: {
        ...o,
        title: t.title,
        description: t.description,
        defaultValue: t.defaultValue,
        optional: t.optional,
        __defaultAssetReference: t.__defaultAssetReference,
      },
    }).control;
}
function L7(t, A) {
  let j = A.cache.get(t);
  if (j !== void 0) return j ?? void 0;
  if (A.resolving.has(t)) return;
  A.resolving.add(t);
  let e = C7(t) ? Bn(t, A) : gj({ control: t }).control;
  return (A.resolving.delete(t), A.cache.set(t, e ?? null), e);
}
function pn(t, A) {
  let j = {};
  for (let e in t) {
    let r = t[e];
    if (!r) continue;
    let o = L7(r, A);
    o && (j[e] = o);
  }
  return j;
}
function w7(t, A) {
  return un({ identifier: t.instanceIdentifier }, A);
}
function un(t, A) {
  if (!D.isOn("crdtModuleControls")) return;
  let j = (o, i) => En(o, A, i),
    e = {
      getStoredPropertyControls: j,
      cache: new Map(),
      parsedPropertyControls: new Map(),
      resolving: new Set(),
      resolvingReferences: new Set(),
    },
    r = j(t, e);
  if (ne(r)) return {};
  if (!_A(r)) return pn(r, e);
}
function J7(t, A) {
  let j = new Set(),
    e = new Set();
  for (let r of A) {
    if (e.has(r)) continue;
    let o = t.get(r);
    if (o) {
      if (eA(o)) {
        j.add(r);
        continue;
      }
      for (let i of o.ancestors())
        if (!e.has(i.id) && (e.add(i.id), i.cache.reset(), !!eA(i))) {
          j.add(i.id);
          break;
        }
    }
  }
  return j;
}
function dk(t) {
  if (Gn(t)) return t.arrayMappingSourceVariable;
}
function Gn(t) {
  return t.providerIds[0] === F6;
}
function mn(t, A, j) {
  let e = [],
    r = [],
    o = (n, s) => {
      if (!Be(n)) return;
      let a = n.getProvidedVariableMap(t, s, A, A.activeBundleHash);
      if (!a) return;
      let k = n.getPrimaryId();
      (r.push(k), e.push(a));
    };
  zA(j) && o(j, void 0);
  let i = j;
  for (let n of j.ancestors()) {
    let s = i;
    if (((i = n), o(n, s), fe(n))) {
      let a = Ie(t);
      a && e.push(a);
    }
  }
  return { variableMaps: e, providerIds: r };
}
var H7 = (() => {
  let t = [],
    A = [],
    j,
    e;
  return (r, o, i, n, s) => {
    if (!i || !s || !n) return null;
    let { variableMaps: a, providerIds: k } = mn(r, o, s);
    if (a.length === 0) return null;
    let l = o.activeBundleHash;
    if (!e || !sA(t, a) || !sA(A, k) || j !== l) {
      let T = function (f, S) {
        if (!Be(f)) return;
        let y = f.getProvidedVariableMap(r, S, o, o.activeBundleHash);
        if (!y) return;
        let I = f.getPrimaryId(),
          Z = f.getVariableSourceIdentifier();
        Z && (F[I] = Z);
        for (let [fA, Q] of y)
          (E.add(fA),
            p.add(Mt(Q)),
            Q.type === "enum" &&
              (d(Z, "Variable source identifier should exist for enum variables"),
              h.push({ variable: Q, providerId: I, sourceIdentifier: Z })),
            !_ &&
              Q.type === "controlReference" &&
              K(G(Q.entityIdentifier)) &&
              !o.componentForIdentifier(Q.entityIdentifier) &&
              (_ = !0));
        (Tt(f) && (R = I), jA(f) && (g = I), $.set(I, y));
        let FA = f.getProvidedControlMap(r, S, o, o.activeBundleHash);
        FA && B.set(I, FA);
      };
      var c = T;
      ((t = a), (A = k), (j = l));
      let $ = new Map(),
        B = new Map(),
        E = new Set(),
        p = new Set(),
        R = null,
        g = null,
        F = {},
        h = [],
        _ = !1,
        b = null;
      zA(s) && T(s, void 0);
      let x = s;
      for (let f of s.ancestors()) {
        let S = x;
        ((x = f), fe(f) && (b = f), T(f, S));
      }
      if (b) {
        let f = Ie(r);
        if (f) {
          F[ZA] = Fe;
          for (let [y, I] of f)
            (E.add(y),
              p.add(I.type),
              d(I.type === "enum"),
              I.type === "enum" && h.push({ variable: I, providerId: ZA, sourceIdentifier: Fe }));
          $.set(ZA, f);
          let S = ht(r);
          (d(S, "Control map should be defined because we received locale variables"),
            B.set(ZA, S));
        }
      }
      e = {
        combined: $,
        combinedControls: B,
        ids: E,
        types: p,
        providerIds: k,
        idOfMutableVariableProvider: R,
        fallbackProvider: g,
        variableSourceIdentifiers: F,
        enums: h,
        hasUnresolvedControlReferenceModules: _,
      };
    }
    return e;
  };
})();
var Wj = class {
  constructor(A, j) {
    this.componentLoader = A;
    this.nodeIgnoredInNodeIdIndex = j;
  }
  componentLoader;
  nodeIgnoredInNodeIdIndex;
  smartComponentNodes = new Set();
  layoutTemplateNodes = new Set();
  codeComponentNodes = new Set();
  shaderNodes = new Set();
  richTextNodes = new Set();
  masterNodes = new Set();
  navigationActionNodes = new Set();
  setVariantActionNodes = new Set();
  formContainerNodes = new Set();
  withDataIdentifierNodes = new Set();
  reset() {
    (this.smartComponentNodes.clear(),
      this.layoutTemplateNodes.clear(),
      this.codeComponentNodes.clear(),
      this.shaderNodes.clear(),
      this.richTextNodes.clear(),
      this.masterNodes.clear(),
      this.navigationActionNodes.clear(),
      this.setVariantActionNodes.clear(),
      this.formContainerNodes.clear(),
      this.withDataIdentifierNodes.clear());
  }
  verifyNode(A, j, e) {
    d(
      A.size === j.size,
      () => `VerificationIndex node ${e} is out of sync: ${A.size} !== ${j.size}`
    );
    for (let r of A) d(j.has(r), "Tree index for " + e + " does not exists");
  }
  update(A) {
    (this.reset(),
      A.forEachNode((j) => {
        if (!this.nodeIgnoredInNodeIdIndex(j)) {
          if (
            (v(j) && this.codeComponentNodes.add(j.id),
            pj(j) && this.shaderNodes.add(j.id),
            bA(j) && this.richTextNodes.add(j.id),
            jA(j) && this.smartComponentNodes.add(j.id),
            DA(j) && this.layoutTemplateNodes.add(j.id),
            yj(j, this.componentLoader) && this.navigationActionNodes.add(j.id),
            Oj(j, this.componentLoader))
          ) {
            let e = NA.getReplicaForTemplateNode(A, j);
            (!e || (e && Gj(e))) && this.setVariantActionNodes.add(j.id);
          }
          (mj(j) && this.formContainerNodes.add(j.id),
            Bj(j) && this.withDataIdentifierNodes.add(j.id));
        }
      }));
  }
  verify({
    smartComponentNodeIds: A,
    layoutTemplateNodeIds: j,
    codeComponentNodeIds: e,
    shaderNodeIds: r,
    richTextNodeIds: o,
    navigationActionNodeIds: i,
    setVariantActionNodeIds: n,
    formContainerNodeIds: s,
    withDataIdentifierNodeIds: a,
  }) {
    (this.verifyNode(this.smartComponentNodes, A, "SmartComponents"),
      this.verifyNode(this.layoutTemplateNodes, j, "LayoutTemplates"),
      this.verifyNode(this.codeComponentNodes, e, "CodeComponents"),
      this.verifyNode(this.shaderNodes, r, "ShaderNodes"),
      this.verifyNode(this.richTextNodes, o, "RichTextNodes"),
      this.verifyNode(this.navigationActionNodes, i, "NavigationActionNodes"),
      this.verifyNode(this.setVariantActionNodes, n, "setVariantActionNodes"),
      this.verifyNode(this.formContainerNodes, s, "formContainerNodes"),
      this.verifyNode(this.withDataIdentifierNodes, a, "withDataIdentifierNodes"));
  }
};
var Fn = 25,
  fn = new (class extends Set {
    has(t) {
      return !0;
    }
  })();
function Yj(t) {
  return t ? Lt(t) || Kt(t) || U(t) || uj(t) || y6(t) || O6(t) : !1;
}
var wA = class {
  constructor(A, j) {
    this.componentLoader = A;
    ((this.verificationIndex = new Wj(A, Yj)),
      (this.reader = j.openNodeChangesReader()),
      this.reset(j.tree));
  }
  componentLoader;
  tree;
  reader;
  verificationIndex;
  onEngineChange;
  layoutTemplateNodeIds = new Set();
  smartComponentNodeIds = new Set();
  codeComponentNodeIds = new Set();
  shaderNodeIds = new Set();
  richTextNodeIds = new Set();
  navigationActionNodeIds = new Set();
  setVariantActionNodeIds = new Set();
  searchComponentNodeIds = new Set();
  formContainerNodeIds = new Set();
  withDataIdentifierNodeIds = new Set();
  deprecatedCodeComponentNodeIds = new Set();
  foundExternalModuleIdentifiers = new Set();
  externalModuleIdentifiersToLoad = new Set();
  nodeIdIndex = new Map();
  activeScopeLinkedNavigationNodes = [];
  activeScopeLinkedVariantNodes = [];
  activeScopeLinkedGestureNodes = [];
  activeScopeCodeComponentArrows = new Set();
  activeScopeId = "";
  activeScopeGroundNodeIds = new Set();
  _shouldChangeSmartComponents = !1;
  _shouldChangeLayoutTemplates = !1;
  shouldChangeCodeComponents = !1;
  shouldChangeShaderNodes = !1;
  shouldChangeRichTextNodes = !1;
  shouldChangeNavigationActions = !1;
  shouldChangeVariantActions = !1;
  shouldChangeActiveScopeGroundNodes = !1;
  shouldChangeWithDataIdentifierNodes = !1;
  get shouldChangeSmartComponents() {
    return this._shouldChangeSmartComponents;
  }
  set shouldChangeSmartComponents(A) {
    ((this._shouldChangeSmartComponents = A),
      A && this.onEngineChange && this.onEngineChange("LocalSmartComponents"));
  }
  get shouldChangeLayoutTemplates() {
    return this._shouldChangeLayoutTemplates;
  }
  set shouldChangeLayoutTemplates(A) {
    ((this._shouldChangeLayoutTemplates = A),
      A && this.onEngineChange && this.onEngineChange("LocalLayoutTemplates"));
  }
  setOnEngineChange(A) {
    this.onEngineChange = A;
  }
  getScopeIdFor(A) {
    if (A) return this.nodeIdIndex.get(A)?.scope;
  }
  getGroundNodeIdFor(A) {
    if (A) return this.nodeIdIndex.get(A)?.ground;
  }
  getNodeIdToCurrentGroundNode(A, j) {
    if (!j) return;
    let e = this.nodeIdIndex.get(j);
    if (e && e.scope === this.activeScopeId) return A.get(e.ground) || void 0;
  }
  addNode(A, j) {
    if (
      (v(j) &&
        ((this.shouldChangeCodeComponents = !0),
        this.codeComponentNodeIds.add(j.id),
        this.handleSearchComponents(j),
        this.handleDeprecatedCodeComponent(j)),
      pj(j) && ((this.shouldChangeShaderNodes = !0), this.shaderNodeIds.add(j.id)),
      mj(j) && this.formContainerNodeIds.add(j.id),
      bA(j) && ((this.shouldChangeRichTextNodes = !0), this.richTextNodeIds.add(j.id)),
      jA(j) && ((this.shouldChangeSmartComponents = !0), this.smartComponentNodeIds.add(j.id)),
      DA(j) && ((this.shouldChangeLayoutTemplates = !0), this.layoutTemplateNodeIds.add(j.id)),
      yj(j, this.componentLoader) &&
        ((this.shouldChangeNavigationActions = !0), this.navigationActionNodeIds.add(j.id)),
      Oj(j, this.componentLoader))
    ) {
      let e = NA.getReplicaForTemplateNode(A, j);
      (!e || (e && Gj(e))) &&
        ((this.shouldChangeVariantActions = !0), this.setVariantActionNodeIds.add(j.id));
    }
    (Bj(j) &&
      ((this.shouldChangeWithDataIdentifierNodes = !0), this.withDataIdentifierNodeIds.add(j.id)),
      this.handleExternalDataSource(j));
  }
  handleSearchComponents(A) {
    let j = G(A.codeComponentIdentifier);
    j && q(j) && j.moduleId === Zr && this.searchComponentNodeIds.add(A.id);
  }
  handleDeprecatedCodeComponent(A) {
    C6(A) && (A.originalid || this.deprecatedCodeComponentNodeIds.add(A.id));
  }
  handleExternalDataSource(A) {
    let j;
    (f6(A) && (j = A.dataIdentifier),
      M6(A) && (j = A.componentIdentifier),
      j &&
        q(j) &&
        (this.foundExternalModuleIdentifiers.has(j) ||
          (this.foundExternalModuleIdentifiers.add(j),
          this.externalModuleIdentifiersToLoad.add(j))));
  }
  getAndClearExternalDataSourcesToLoad() {
    if (this.externalModuleIdentifiersToLoad.size === 0) return null;
    let A = Array.from(this.externalModuleIdentifiersToLoad);
    return (this.externalModuleIdentifiersToLoad.clear(), A);
  }
  deleteNode(A) {
    (this.smartComponentNodeIds.delete(A) && (this.shouldChangeSmartComponents = !0),
      this.layoutTemplateNodeIds.delete(A) && (this.shouldChangeLayoutTemplates = !0),
      this.codeComponentNodeIds.delete(A) && (this.shouldChangeCodeComponents = !0),
      this.shaderNodeIds.delete(A) && (this.shouldChangeShaderNodes = !0),
      this.richTextNodeIds.delete(A) && (this.shouldChangeRichTextNodes = !0),
      this.navigationActionNodeIds.delete(A) && (this.shouldChangeNavigationActions = !0),
      this.setVariantActionNodeIds.delete(A) && (this.shouldChangeVariantActions = !0),
      this.withDataIdentifierNodeIds.delete(A) && (this.shouldChangeWithDataIdentifierNodes = !0),
      this.activeScopeGroundNodeIds.delete(A) && (this.shouldChangeActiveScopeGroundNodes = !0),
      this.formContainerNodeIds.delete(A),
      this.nodeIdIndex.delete(A),
      this.searchComponentNodeIds.delete(A),
      this.deprecatedCodeComponentNodeIds.delete(A));
  }
  reset(A) {
    (this.smartComponentNodeIds.clear(),
      this.layoutTemplateNodeIds.clear(),
      this.codeComponentNodeIds.clear(),
      this.shaderNodeIds.clear(),
      this.richTextNodeIds.clear(),
      this.navigationActionNodeIds.clear(),
      this.setVariantActionNodeIds.clear(),
      this.activeScopeGroundNodeIds.clear(),
      this.nodeIdIndex.clear(),
      this.searchComponentNodeIds.clear(),
      this.deprecatedCodeComponentNodeIds.clear(),
      this.formContainerNodeIds.clear(),
      this.withDataIdentifierNodeIds.clear(),
      (this.tree = A),
      A.beginAllowPartialScopeAccess(),
      A.forEachNode((j) => {
        Yj(j) || (this.addNode(A, j), this.updateNodeIdIndex(A, j));
      }),
      A.endAllowPartialScopeAccess(),
      this.reader.read(),
      this.setAllChangeFlags());
  }
  setAllChangeFlags() {
    ((this.shouldChangeSmartComponents = !0),
      (this.shouldChangeLayoutTemplates = !0),
      (this.shouldChangeRichTextNodes = !0),
      (this.shouldChangeCodeComponents = !0),
      (this.shouldChangeShaderNodes = !0),
      (this.shouldChangeNavigationActions = !0),
      (this.shouldChangeVariantActions = !0),
      (this.shouldChangeActiveScopeGroundNodes = !0),
      (this.shouldChangeWithDataIdentifierNodes = !0));
  }
  getNodes(A) {
    let j = [];
    for (let e of A) {
      let r = this.tree.get(e);
      r && j.push(r);
    }
    return j;
  }
  getNodesForActiveScope(A) {
    let j = [];
    for (let e of A) {
      if (this.getScopeIdFor(e) !== this.activeScopeId) continue;
      let r = this.tree.get(e);
      r && j.push(r);
    }
    return j;
  }
  processActiveScope(A, j) {
    if (this.activeScopeId === j || j.length === 0) return;
    ((this.activeScopeId = j),
      this.activeScopeGroundNodeIds.clear(),
      (this.shouldChangeActiveScopeGroundNodes = !0));
    let e = A.get(this.activeScopeId);
    if (!(!eA(e) || !e.isLoaded()))
      for (let r of e.loaded.children) this.activeScopeGroundNodeIds.add(r.id);
  }
  processReset(A) {
    (this.processActiveScope(A, ""), this.reset(A), this.verify());
  }
  process(A, j) {
    if ((this.processActiveScope(A, j), this.tree.lineage !== A.lineage)) {
      (this.reset(A), this.verify());
      return;
    }
    this.tree = A;
    let e = [],
      r = [];
    for (let [o, i] of this.reader.read()) {
      let n = A.get(o);
      if (!Yj(n) && (this.deleteNode(o), n && (this.addNode(A, n), e.push(n), !eA(n)))) {
        for (let s of i)
          if (!s.added && s.to.parentid) {
            r.push(n);
            break;
          }
      }
    }
    for (let o of e) this.updateNodeIdIndex(A, o);
    for (let o of r) this.updateNodeIdIndexForMovedNode(o);
    this.verify();
  }
  updateNodeIdIndexForMovedNode(A) {
    if (!A.children) return;
    let j = this.nodeIdIndex.get(A.id);
    for (let e of A.children) for (let r of e.walk()) this.nodeIdIndex.set(r.id, j);
  }
  updateNodeIdIndex(A, j) {
    let e = this.nodeIdIndex.get(j.id);
    if (e) return e;
    if (eA(j)) {
      let i = { scope: j.id, ground: j.id };
      (this.nodeIdIndex.set(j.id, i),
        j.id === this.activeScopeId && (this.shouldChangeActiveScopeGroundNodes = !0));
      return;
    }
    let r = A.get(j.parentid);
    if (!r) return;
    if (eA(r) || h6(r)) {
      let i = { scope: r.id, ground: j.id };
      return (
        this.nodeIdIndex.set(j.id, i),
        ae(j) ||
          (r.id === this.activeScopeId &&
            ((this.shouldChangeActiveScopeGroundNodes = !0),
            this.activeScopeGroundNodeIds.add(j.id))),
        i
      );
    }
    let o = this.updateNodeIdIndex(A, r);
    if (o) return (this.nodeIdIndex.set(j.id, o), o);
  }
  updateCodeComponents() {
    this.tree.beginAllowPartialScopeAccess();
    for (let A of this.codeComponentNodeIds) {
      (this.navigationActionNodeIds.delete(A) && (this.shouldChangeNavigationActions = !0),
        this.setVariantActionNodeIds.delete(A) && (this.shouldChangeVariantActions = !0));
      let j = this.tree.get(A);
      j && this.addNode(this.tree, j);
    }
    this.tree.endAllowPartialScopeAccess();
  }
  verify() {}
  verifyScope(A, j) {
    let e = 0;
    for (let r of j.walk()) {
      if (Yj(r)) continue;
      e += 1;
      let o = this.nodeIdIndex.get(r.id);
      (d(o, () => `id ${r.id} does not exist in index`),
        d(o.scope === A.id, () => `${r.id} scope is: ${o.scope} should be: ${A.id}`),
        d(o.ground === j.id, () => `${r.id} ground is: ${o.ground} should be: ${j.id}`));
    }
    return e;
  }
  updateConnectedScreens(A, j, e) {
    let r = new Set();
    if (e) r = fn;
    else {
      for (let a of j) {
        let k = this.getNodeIdToCurrentGroundNode(A, a);
        k && r.add(k);
      }
      r.size > Fn && r.clear();
    }
    let o = this.getLinkedNavigationNodes(A, r);
    sA(this.activeScopeLinkedNavigationNodes, o) || (this.activeScopeLinkedNavigationNodes = o);
    let i = this.getLinkedVariantNodes(A, j, r, e);
    sA(this.activeScopeLinkedVariantNodes, i) || (this.activeScopeLinkedVariantNodes = i);
    let n = this.getLinkedGestureNodes(A, r);
    sA(this.activeScopeLinkedGestureNodes, n) || (this.activeScopeLinkedGestureNodes = n);
    let s = this.getCodeComponentArrows(A, r);
    H6(this.activeScopeCodeComponentArrows, s) || (this.activeScopeCodeComponentArrows = s);
  }
  getLinkedVariantNodes(A, j, e, r) {
    let o = [],
      i = new Set(),
      n = j[0];
    if (!r && (j.length !== 1 || !n)) return o;
    for (let s of this.setVariantActionNodeIds) {
      if (this.nodeIdIndex.get(s)?.scope !== this.activeScopeId) continue;
      let a = this.getNodeIdToCurrentGroundNode(A, s);
      if (!a || (!r && n !== a.id && n !== s)) continue;
      let k = A.get(s);
      if (!k) continue;
      (i.clear(),
        ir(A, k, this.componentLoader).forEach((c) => {
          if (i.has(c)) return;
          let $ = A.getNode(c);
          if (!$ || !pA($)) return;
          let B = this.getNodeIdToCurrentGroundNode(A, $.id);
          B === $ &&
            B !== a &&
            B.parentid === a.parentid &&
            (e.has(a) || e.has(B)) &&
            (i.add($.id), o.push({ fromNode: k, toNode: $ }));
        }));
    }
    return o;
  }
  getLinkedNavigationNodes(A, j) {
    let e = [],
      r = new Set();
    for (let o of this.navigationActionNodeIds) {
      if (this.nodeIdIndex.get(o)?.scope !== this.activeScopeId) continue;
      let i = this.getNodeIdToCurrentGroundNode(A, o);
      if (!i) continue;
      r.clear();
      let n = A.get(o);
      ye(n, this.componentLoader).forEach((a) => {
        if (
          !a.controls ||
          !a.controls.target ||
          !a.controls.target.value ||
          !a.controls.type ||
          a.controls.type.value !== "next"
        )
          return;
        let k = A.getNode(a.controls.target.value);
        if (!k || r.has(k.id)) return;
        let l = this.getNodeIdToCurrentGroundNode(A, k.id);
        l &&
          l === k &&
          l !== i &&
          l.parentid === i.parentid &&
          ((j.has(i) || j.has(l)) && e.push({ fromNode: n, toNode: k }), r.add(k.id));
      });
    }
    return e;
  }
  getLinkedGestureNodes(A, j) {
    let e = [],
      r = A.get(this.activeScopeId);
    for (let o of r?.children ?? []) {
      if (!x6(o) || !o.replicaInfo.inheritsFrom) continue;
      let n = A.getNode(o.replicaInfo.inheritsFrom);
      if (!n || !pA(n)) continue;
      let s = this.getNodeIdToCurrentGroundNode(A, o.id),
        a = this.getNodeIdToCurrentGroundNode(A, n.id);
      (j.has(a) || j.has(s)) && e.push({ fromNode: o, toNode: n });
    }
    return e;
  }
  getCodeComponentArrows(A, j) {
    let e = new Set(),
      r = A.get(this.activeScopeId);
    if (!eA(r)) return e;
    for (let o of this.codeComponentNodeIds) {
      if (this.nodeIdIndex.get(o)?.scope !== this.activeScopeId) continue;
      let i = this.getNodeIdToCurrentGroundNode(A, o);
      if (!i) continue;
      let n = A.get(o);
      if (!n || (d(v(n), "bug"), n.slotsAreChildNodes)) continue;
      if (j.has(i)) {
        e.add(n);
        continue;
      }
      let a = this.componentLoader.reactComponentForIdentifier(n.codeComponentIdentifier);
      if (!a) continue;
      let k = n.getControlProps(a.properties);
      Object.values(k).some((c) => {
        if (c.type !== "slot") return !1;
        for (let $ of c.value) {
          let B = A.get($.reference);
          if (!or(r.id, B)) continue;
          let E = this.getNodeIdToCurrentGroundNode(A, B.id);
          if (E && E !== i && E.parentid === i.parentid && j.has(E)) return !0;
        }
        return !1;
      }) && e.add(n);
    }
    return e;
  }
  prototypeScreenLinksTree = null;
  prototypeScreenLinks = {};
  getPrototypeScreenLinks(A, j) {
    let e = this.getScopeIdFor(j);
    if (!e) return [];
    this.prototypeScreenLinksTree !== A &&
      ((this.prototypeScreenLinksTree = A), (this.prototypeScreenLinks = {}));
    let r = this.prototypeScreenLinks[e];
    if (r) return r;
    let o = [],
      i = new Set();
    for (let n of this.navigationActionNodeIds) {
      let s = this.nodeIdIndex.get(n);
      if (!s || s.scope !== e) continue;
      let a = A.get(s.ground);
      if (!a) continue;
      let k = A.get(n),
        l = ye(k, this.componentLoader);
      (i.clear(),
        l.forEach((c) => {
          if (
            !c.controls ||
            !c.controls.target ||
            !c.controls.target.value ||
            !c.controls.type ||
            c.controls.type.value !== "next"
          )
            return;
          let $ = A.getNode(c.controls.target.value);
          if (!$ || i.has($.id)) return;
          let B = A.get(this.getGroundNodeIdFor($.id));
          B &&
            B === $ &&
            B !== a &&
            B.parentid === a.parentid &&
            (i.add($.id), o.push({ fromNode: a.id, toNode: $.id }));
        }));
    }
    for (let n of this.codeComponentNodeIds) {
      let s = this.nodeIdIndex.get(n);
      if (!s || s.scope !== e) continue;
      let a = A.get(s.ground);
      if (!a) continue;
      let k = A.get(n);
      if (!k || (d(v(k), "bug"), CA(k))) continue;
      let l = rr(k, this.componentLoader);
      if (l.length !== 0)
        for (let c of l) {
          let B = k.getRawControlProp(c)?.value;
          !B ||
            !Array.isArray(B) ||
            B.forEach((E) => {
              if (typeof E.value != "string") return;
              let p = A.get(E.value);
              p && (pA(p) || o.push({ fromNode: a.id, toNode: p.id }));
            });
        }
    }
    return ((this.prototypeScreenLinks[e] = o), o);
  }
};
var A6 = BA("treeStore");
function gn(t) {
  return AA(t) && typeof t.then == "function";
}
function hn(t) {
  return AA(t) && typeof t.then == "function";
}
function _n() {
  if (!("treeInitData" in window) || !AA(window.treeInitData)) return;
  let t = window.treeInitData,
    {
      mode: A,
      version: j,
      file: e,
      prefetchPromise: r,
      prefetchSyncPromise: o,
      syncURL: i,
      socketURL: n,
    } = t;
  delete window.treeInitData;
  let s = m(i) ? { syncURL: i } : {},
    a = J6(A) ? { mode: A } : {};
  if (r && hn(r)) {
    if (!m(n) || !W(j) || !m(e)) {
      let k = Object.keys(t);
      A6.error("Expected socketURL, version, and file in window.treeInitData but got:", k);
      return;
    }
    return { version: j, file: e, prefetchPromise: r, socketURL: n, ...a, ...s };
  } else if (o && gn(o)) {
    if (!m(n)) {
      let k = Object.keys(t);
      A6.error("Expected socketURL in window.treeInitData but got:", k);
      return;
    }
    return { prefetchSyncPromise: o, socketURL: n, ...a, ...s };
  }
  if ((A6.warn("Not using prefetch."), m(n))) return { socketURL: n, ...a, ...s };
}
var V7, Zj, j6;
V7 = [uA()];
var OA = class {
  constructor(A, j, e, r, o, i, n, s, a, k, l) {
    this.scheduler = A;
    this.addChanges = j;
    this.componentLoader = e;
    this.persistedUserDefaults = r;
    this.chromeStore = o;
    this.guidesStore = i;
    this.undoRedoManager = n;
    this.layoutCache = s;
    this.getEffectCacheUpdate = a;
    this.waitForComponentLoaderToCatchUp = k;
    u(this, "oldSelection", []);
    u(this, "oldSelectedGradientStopId");
    u(this, "oldScopeId", null);
    u(this, "projectId");
    u(this, "pendingCacheUpdateNodeIds", new Set());
    u(this, "initData", _n());
    (V(this, j6, H(Zj, 8, this)), H(Zj, 11, this));
    u(this, "timeline");
    u(this, "highlight");
    u(this, "collapseOverrides", new Map());
    u(this, "showAllArrows", !1);
    u(this, "isDocumentEmpty", !0);
    u(this, "visibleGroundNodesInActiveScope", []);
    u(this, "groundNodesInViewport", new Set());
    u(this, "_codeComponents", null);
    u(this, "_richTextNodes", null);
    u(this, "_shaderNodes", null);
    u(this, "_localSmartComponentNameByIdentifier", null);
    u(this, "_localLayoutTemplatesNameByIdentifier", null);
    u(this, "activeScopeCodeComponentArrows", new Set());
    u(this, "activeScopeLinkedNavigationNodes", []);
    u(this, "activeScopeLinkedVariantNodes", []);
    u(this, "activeScopeLinkedGestureNodes", []);
    u(this, "allNodesShareVariableContext", !1);
    u(this, "variableProviderInfo", null);
    u(this, "pendingSelectionForControlReferenceModulesLoad");
    u(this, "crashSoon", !1);
    u(this, "sandboxRepeaterData", new Xr("editor"));
    u(this, "sandboxVisibility", new Qr("editor"));
    u(this, "localOnlyOverlays", {
      write: (A, j) => this.writeLocalOnlyOverlay(A, j),
      promote: (A) => this.promoteLocalOnlyOverlay(A),
      discard: (A) => this.discardLocalOnlyOverlay(A),
      getRows: () => this.getLocalOnlyOverlayRows(),
    });
    u(this, "domLayoutUpdate", 0);
    u(this, "treeIndex");
    u(this, "initialTree");
    let c = window.bootstrap?.project?.id;
    if (c) this.projectId = c;
    else if (!(hA.isAutomation || hA.isTest)) throw Error("Can't find project id");
    let $ = er(l?.mode ?? this.initData?.mode);
    ((this.tree = nr.createWithDefaultWebPage({ ...l, mode: $ })),
      (this.initialTree = this.tree),
      this.tree.setService("treeQueryContext", this),
      (this.timeline = this.createTimeline($)),
      (this.treeIndex = new wA(e, this.timeline)),
      this.treeIndex.setOnEngineChange((B) => this.addChanges(B)),
      this.assertTimelineMatchesMode(),
      this.setTree(this.tree),
      this.setShowAllArrows(r.displayAllLinks, []));
  }
  scheduler;
  addChanges;
  componentLoader;
  persistedUserDefaults;
  chromeStore;
  guidesStore;
  undoRedoManager;
  layoutCache;
  getEffectCacheUpdate;
  waitForComponentLoaderToCatchUp;
  get codeComponents() {
    return (
      this.treeIndex.shouldChangeCodeComponents &&
        ((this.treeIndex.shouldChangeCodeComponents = !1),
        (this._codeComponents = this.treeIndex.getNodes(this.treeIndex.codeComponentNodeIds))),
      this._codeComponents ?? []
    );
  }
  get shaderNodes() {
    return (
      this.treeIndex.shouldChangeShaderNodes &&
        ((this.treeIndex.shouldChangeShaderNodes = !1),
        (this._shaderNodes = this.treeIndex.getNodes(this.treeIndex.shaderNodeIds))),
      this._shaderNodes ?? []
    );
  }
  get richTextNodes() {
    return (
      this.treeIndex.shouldChangeRichTextNodes &&
        ((this.treeIndex.shouldChangeRichTextNodes = !1),
        (this._richTextNodes = this.treeIndex.getNodes(this.treeIndex.richTextNodeIds))),
      this._richTextNodes ?? []
    );
  }
  get formContainerNodeIds() {
    return this.treeIndex.formContainerNodeIds;
  }
  get withDataIdentifierNodeIds() {
    return this.treeIndex.withDataIdentifierNodeIds;
  }
  get localSmartComponentNameByIdentifier() {
    if (this.treeIndex.shouldChangeSmartComponents) {
      this.treeIndex.shouldChangeSmartComponents = !1;
      let A = {};
      for (let e of this.treeIndex.smartComponentNodeIds) {
        let r = this.tree.get(e);
        r && (A[r.instanceIdentifier] = r.resolveValue("name"));
      }
      let j = this._localSmartComponentNameByIdentifier ?? {};
      ue(j, A) || (this._localSmartComponentNameByIdentifier = A);
    }
    return this._localSmartComponentNameByIdentifier ?? {};
  }
  get localLayoutTemplatesNameByIdentifier() {
    if (this.treeIndex.shouldChangeLayoutTemplates) {
      this.treeIndex.shouldChangeLayoutTemplates = !1;
      let A = {};
      for (let e of this.treeIndex.layoutTemplateNodeIds) {
        let r = this.tree.get(e);
        r && (A[r.instanceIdentifier] = r.resolveValue("name"));
      }
      let j = this._localLayoutTemplatesNameByIdentifier ?? {};
      ue(j, A) || (this._localLayoutTemplatesNameByIdentifier = A);
    }
    return this._localLayoutTemplatesNameByIdentifier ?? {};
  }
  get mode() {
    return zr(this);
  }
  set mode(A) {
    Wr(this, A, {
      initialTree: this.initialTree,
      createTimeline: (j) => this.createTimeline(j),
      resetTimeline: !0,
    }) &&
      ((this.treeIndex = new wA(this.componentLoader, this.timeline)),
      this.treeIndex.setOnEngineChange((j) => this.addChanges(j)),
      this.assertTimelineMatchesMode(),
      this.addChanges("TreeStore"));
  }
  createTimeline(A) {
    let { projectId: j } = this,
      e = j ? (r, o) => Cr().setSeqWatermark(j, r, o) : void 0;
    return A === "crdt"
      ? new xr(this.tree, this.componentLoader, { afterLocalWrite: e })
      : new Dr(this.tree, this.componentLoader);
  }
  assertCrdtTimeline() {
    (d(this.mode === "crdt", "CRDT timeline requested for non-CRDT tree mode"),
      d(ve(this.timeline), "CRDT tree mode requires a CRDT timeline"));
  }
  get crdtTimeline() {
    return (this.assertCrdtTimeline(), this.timeline);
  }
  resolvePropertyControls(A) {
    return (
      d(this.mode === "crdt", "CRDT tree mode is required to resolve property controls"),
      w7(A, this.crdtTimeline.store)
    );
  }
  assertTimelineMatchesMode() {
    let A = ve(this.timeline);
    d(this.mode === "crdt" ? A : !A, "Tree mode and timeline type are out of sync");
  }
  prepareDetachedRootReplacement(A) {
    (this.mode === "crdt" &&
      this.crdtTimeline.branches.activeBranchId !== ej &&
      this.crdtTimeline.switchActiveBranch(ej),
      this.timeline.reset(A),
      this.mode === "crdt" && this.resetCrdtStoreToTree(A),
      (this.treeIndex = new wA(this.componentLoader, this.timeline)),
      this.treeIndex.setOnEngineChange((j) => this.addChanges(j)));
  }
  resetCrdtStoreToTree(A) {
    let { store: j } = this.crdtTimeline;
    (j.reset(),
      j.importObject(A.root.id, A.root.toJS()),
      j.setRoot(A.root.id),
      j.setVersion(A.serializationVersion),
      this.crdtTimeline.branches.reset(),
      this.crdtTimeline.resetBranchDrainStatesToActiveStoreHead());
  }
  ensureMainCrdtStoreInitialized() {
    this.mode === "crdt" &&
      this.isOnMainBranch() &&
      (this.crdtTimeline.store.length > 0 || this.resetCrdtStoreToTree(this.tree));
  }
  getLoadedIndexes() {
    let A = this.tree.getService("loader");
    return (
      d(
        !A,
        () => `Some scopes are still loading: ${A?.numberOfScopesToLoad()} scopes left to load`
      ),
      this.getPartialIndexes()
    );
  }
  getPartialIndexes() {
    return this;
  }
  getDataTree() {
    return this.timeline.getOrCreateDataTree(this);
  }
  resetDataTree() {
    this.timeline.resetDataTree();
  }
  getDataTreeOrLoadedTree() {
    let A = this.getDataTree();
    return A
      ? A.getCanvasTreeProxy()
      : (d(!this.tree.getService("loader"), "Tree must be fully loaded"), this.tree);
  }
  getDataTreeOrPartialTree() {
    let A = this.getDataTree();
    return A ? A.getCanvasTreeProxy() : this.tree;
  }
  query() {
    return new jr(this);
  }
  updateGroundNodesList() {
    if (!this.treeIndex.shouldChangeActiveScopeGroundNodes) return;
    this.treeIndex.shouldChangeActiveScopeGroundNodes = !1;
    let A = this.treeIndex.getNodes(this.treeIndex.activeScopeGroundNodeIds).filter(me);
    if (
      (sA(this.visibleGroundNodesInActiveScope, A) || (this.visibleGroundNodesInActiveScope = A),
      A.length > 0)
    ) {
      this.isDocumentEmpty = !1;
      return;
    } else
      for (let j of this.tree.root.children)
        if (!(eA(j) && !j.isValid()) && j.children) {
          for (let e of j.children)
            if (me(e)) {
              this.isDocumentEmpty = !1;
              return;
            }
        }
    this.isDocumentEmpty = !0;
  }
  get initialSnapshotVersion() {
    return this.initData?.version;
  }
  get isNewDocument() {
    let A = this.initialSnapshotVersion;
    return W(A) && A <= 2;
  }
  get initialFileURL() {
    return this.initData?.file;
  }
  get initialSocketURL() {
    return this.initData?.socketURL;
  }
  get serverHistoryURL() {
    let A = `/projects/${this.projectId}/history`;
    return this.mode === "crdt" ? Yr(A) : `/internal${A}`;
  }
  getSocketURL(A) {
    return this.initialSocketURL ? this.initialSocketURL : A;
  }
  getDocumentURL(A) {
    return A || this.initialFileURL;
  }
  setTree(A, j = {}) {
    let { mode: e = "reset", clearUndo: r = !0 } = j;
    ((this.tree = A),
      this.tree.setService("treeQueryContext", this),
      pe(this.tree, this.mode),
      (A.inEditor = !0),
      this.assertTimelineMatchesMode(),
      e === "replace" && this.mode === "crdt"
        ? this.crdtTimeline.replaceTree(A)
        : this.timeline.reset(A),
      (this.highlight = $e),
      (this.collapseOverrides = new Map()),
      this.tree.resetDraftNodesAndCache(),
      r && this.undoRedoManager.clearUndoStack(),
      this.addChanges("Tree"),
      this.addChanges("ColorStyleTokens"),
      this.addChanges("TextStylePresets"),
      d(A.root.children.at(0), "Tree must not be empty"),
      this.treeIndex.processReset(A),
      this.updateGroundNodesList());
  }
  hasUncommittedChanges() {
    return this.tree.hasUncommittedChanges() || !!this.getDataTree()?.hasUncommittedChanges();
  }
  commit() {
    this.ensureMainCrdtStoreInitialized();
    let A = this.timeline.commitLocalTree();
    return (
      this.pendingCacheUpdateNodeIds.size > 0 &&
        (this.updateTreeCacheForNodes(
          this.pendingCacheUpdateNodeIds,
          this.componentLoader.activeBundleHash,
          A.tree
        ),
        this.pendingCacheUpdateNodeIds.clear()),
      A
    );
  }
  writeLocalOnlyOverlay(A, j) {
    return this.mode !== "crdt" || !this.crdtTimeline.writeLocalOnlyOverlay(A, j)
      ? !1
      : (this.installTimelineTreeAfterLocalOnlyOverlay(), !0);
  }
  promoteLocalOnlyOverlay(A) {
    return this.mode !== "crdt" || !this.crdtTimeline.promoteLocalOnlyOverlay(A)
      ? !1
      : (this.installTimelineTreeAfterLocalOnlyOverlay(), !0);
  }
  discardLocalOnlyOverlay(A) {
    return this.mode !== "crdt" || !this.crdtTimeline.discardLocalOnlyOverlay(A)
      ? !1
      : (this.installTimelineTreeAfterLocalOnlyOverlay(), !0);
  }
  getLocalOnlyOverlayRows() {
    return this.mode !== "crdt" ? oA() : this.crdtTimeline.getLocalOnlyOverlayRows();
  }
  getTreeForPostProcessing(A = this.tree) {
    return A.latestTree();
  }
  installTimelineTreeAfterLocalOnlyOverlay() {
    ((this.tree = this.crdtTimeline.tree),
      this.tree.setService("treeQueryContext", this),
      pe(this.tree, this.mode),
      (this.tree.inEditor = !0),
      this.tree.resetDraftNodesAndCache(),
      this.treeIndex.processReset(this.tree),
      this.updateGroundNodesList(),
      this.addChanges("Tree"),
      this.addChanges("TreeStore"));
  }
  syncNodeCacheWithDOMLayout(A) {
    hr(this.tree, this.layoutCache, A).size > 0 &&
      (this.domLayoutUpdate++, this.addChanges("TreeDOMLayoutSync"));
  }
  clearStaleDOMLayoutCacheForOutOfScopeNodes(A) {
    if (A.size !== 0)
      for (let j of A) {
        let e = this.tree.get(j);
        !e || !L6(e) || e.invalidateDOMRect();
      }
  }
  updateTreeCacheForNodes(A, j, e = this.tree) {
    if (A.size === 0) return;
    if (e.hasUncommittedChanges()) {
      for (let o of A) this.pendingCacheUpdateNodeIds.add(o);
      return;
    }
    let r = J7(e, A);
    (this.addChanges("TreeStore"),
      gr(
        e,
        this.layoutCache,
        this.sandboxRepeaterData,
        this.sandboxVisibility,
        this.componentLoader,
        j,
        this.chromeStore.canvasLocaleId,
        this.getEffectCacheUpdate(),
        r
      ));
  }
  updateConnectedScreens(A) {
    (this.treeIndex.updateConnectedScreens(this.tree, A, this.showAllArrows),
      (this.activeScopeLinkedNavigationNodes !== this.treeIndex.activeScopeLinkedNavigationNodes ||
        this.activeScopeLinkedVariantNodes !== this.treeIndex.activeScopeLinkedVariantNodes ||
        this.activeScopeLinkedGestureNodes !== this.treeIndex.activeScopeLinkedGestureNodes ||
        this.activeScopeCodeComponentArrows !== this.treeIndex.activeScopeCodeComponentArrows) &&
        ((this.activeScopeLinkedNavigationNodes = this.treeIndex.activeScopeLinkedNavigationNodes),
        (this.activeScopeLinkedVariantNodes = this.treeIndex.activeScopeLinkedVariantNodes),
        (this.activeScopeLinkedGestureNodes = this.treeIndex.activeScopeLinkedGestureNodes),
        (this.activeScopeCodeComponentArrows = this.treeIndex.activeScopeCodeComponentArrows),
        this.addChanges("Arrows")));
  }
  updateVariableProviderInfo(A) {
    this.pendingSelectionForControlReferenceModulesLoad !== void 0 &&
      (this.pendingSelectionForControlReferenceModulesLoad = A);
    let j = this.tree.getNodes(A),
      e = j.map((s) =>
        zA(s)
          ? s.cache.getUnsafeCombinedVariableValueMap()
          : s.cache.getUnsafeInheritedCombinedVariableValueMap()
      ),
      r = j.every((s) => s.supportsDynamicValues()),
      o = new Set(e).size === 1,
      i = j[0];
    this.allNodesShareVariableContext = o;
    let n = this.variableProviderInfo;
    ((this.variableProviderInfo = H7(this.tree, this.componentLoader, o, r, i)),
      n !== this.variableProviderInfo &&
        (this.addChanges("VariableProviderInfo"), this.ensureControlReferenceModulesLoaded(A)));
  }
  ensureControlReferenceModulesLoaded(A) {
    this.variableProviderInfo?.hasUnresolvedControlReferenceModules &&
      this.pendingSelectionForControlReferenceModulesLoad === void 0 &&
      this.waitForComponentLoaderToCatchUp &&
      ((this.pendingSelectionForControlReferenceModulesLoad = A),
      this.waitForComponentLoaderToCatchUp()
        .then(() => {
          let j = this.pendingSelectionForControlReferenceModulesLoad;
          ((this.pendingSelectionForControlReferenceModulesLoad = void 0),
            j && this.updateVariableProviderInfo(j));
        })
        .catch(() => {
          this.pendingSelectionForControlReferenceModulesLoad = void 0;
        }));
  }
  updateCodeComponents() {
    this.treeIndex.updateCodeComponents();
  }
  updateGroundNodesInViewport(A) {
    let j = new Set(),
      e = this.groundNodesInViewport,
      r = this.visibleGroundNodesInActiveScope,
      o = !0;
    for (let i of r) {
      let n = i.id;
      nA.intersects(jt(i), A) && (j.add(n), o && !e.has(n) && (o = !1));
    }
    (!o || j.size !== e.size) && (this.groundNodesInViewport = j);
  }
  selectionDidChange(A) {
    let j = this.tree;
    for (let r in this.collapseOverrides) j.has(r) || this.deleteCollapseOverride(r);
    if (
      (j.has(this.highlight) || (this.highlight = $e),
      A.length > 0 && this.collapseOverrides.size > 0)
    ) {
      let r = Pr(A, this.oldSelection),
        o = [];
      (r.forEach((i) => {
        o.push(...j.getAncestors(i));
      }),
        o.forEach((i) => {
          this.deleteCollapseOverride(i);
        }));
    }
    let e = this.oldSelection.length !== A.length;
    for (let r of A) {
      let o = this.tree.getNode(r);
      o && (o.cache.selected || ((e = !0), (o.cache.selected = !0)));
    }
    if (e)
      for (let r of this.oldSelection) {
        if (A.includes(r)) continue;
        let o = j.getNode(r);
        o && (o.cache.selected = !1);
      }
  }
  processLatestChangesForUndo() {
    let A = this.timeline.getLatestChangesForUndo();
    if (!A) return [];
    let j = [];
    for (let e of A) {
      e.ignoreInUndo || j.push(e);
      let r = this.tree.get(e.id);
      ((b6(r) || S6(r)) && this.addChanges("ColorStyleTokens"),
        (P6(r) || v6(r)) && this.addChanges("TextStylePresets"));
      let o = this.persistedUserDefaults.frameBackground;
      e.removed === "ColorStyleTokenNode" &&
        o &&
        o.includes(e.id) &&
        (this.persistedUserDefaults.frameBackground = null);
    }
    return j;
  }
  discardUndoRedoEntriesForNode(A) {
    this.undoRedoManager.removeEntriesForNode(A);
  }
  postProcess(A, j, e, r, o, i) {
    ((A = this.getTreeForPostProcessing(A)),
      d(!A.hasUncommittedChanges(), "tree must be clean"),
      d(A.isLatest(), "tree must be latest"),
      A.lineage.setActiveChangeset(void 0),
      this.resetDataTree());
    let n = this.tree !== A,
      s = !1;
    if (
      n &&
      ((s = A.get(r) !== this.tree.get(r)),
      (this.tree = A),
      this.addChanges("Tree"),
      this.crashSoon)
    )
      throw ((this.crashSoon = !1), Error("TreeStore CrashTest"));
    this.tree.beginAllowPartialScopeAccess();
    let a = this.oldSelection !== j;
    if ((a && this.selectionDidChange(j), n)) {
      let k = this.processLatestChangesForUndo();
      if (
        (e &&
          k.length > 0 &&
          this.undoRedoManager.addUndoEntry(
            new sr(k, {
              branchId: this.branchId,
              selection: this.oldSelection,
              scopeId: this.oldScopeId ?? r,
              localeId: o,
              selectedGradientStopId: this.oldSelectedGradientStopId,
            })
          ),
        this.addChanges("TreeStore"),
        k.length > 0)
      ) {
        let l = this.layoutCache.pruneAfterTreeChanges(k, this.tree, r);
        this.clearStaleDOMLayoutCacheForOutOfScopeNodes(l);
      }
      this.treeIndex.process(A, r);
    } else this.treeIndex.processActiveScope(A, r);
    (this.updateGroundNodesList(),
      (s || a) && this.updateConnectedScreens(j),
      (n || a) && this.updateVariableProviderInfo(j),
      (this.oldSelection = j),
      (this.oldScopeId = r),
      (this.oldSelectedGradientStopId = i),
      this.undoRedoManager.processScheduledEndUndoGroup({
        branchId: this.branchId,
        selection: j,
        scopeId: r,
        localeId: o,
        selectedGradientStopId: i,
      }),
      this.tree.endAllowPartialScopeAccess());
  }
  setHighlight(A) {
    this.highlight !== A &&
      ((this.highlight = A),
      A && (this.guidesStore.highlighted = void 0),
      this.addChanges("TreeStore", "Highlight"));
  }
  setCollapseOverride(A, j) {
    let e = new Map(this.collapseOverrides);
    (e.set(A, j), (this.collapseOverrides = e));
  }
  deleteCollapseOverride(A) {
    if (!this.collapseOverrides.has(A)) return;
    let j = new Map(this.collapseOverrides);
    (j.delete(A), (this.collapseOverrides = j));
  }
  setCollapseMode(A, j, e) {
    if (e) this.setCollapseOverride(A, j);
    else if (this.collapseOverrides.has(A)) this.deleteCollapseOverride(A);
    else return;
    this.addChanges("TreeStore");
  }
  collapseLayers(A) {
    let j = new Map();
    for (let e of A) {
      let r = this.tree.get(e);
      if (r && r.children) {
        for (let o of r.children) j.set(o.id, !0);
        j.set(e, !0);
      }
    }
    ((this.collapseOverrides = j), this.addChanges("TreeStore"));
  }
  resetCollapseOverrides() {
    ((this.collapseOverrides = new Map()), this.addChanges("TreeStore"));
  }
  setShowAllArrows(A, j) {
    this.showAllArrows !== A &&
      ((this.showAllArrows = A), this.updateConnectedScreens(j), this.addChanges("TreeStore"));
  }
  resetTreeWithToken(A, j) {
    let e = this.undoRedoManager.resetTreeWithToken(this.timeline, A, j);
    if (!e) return;
    let { selection: r, changedGroundNodes: o } = e;
    for (let i of o) this.unsafeUpdateNodeCache(i, !0, !1);
    return r;
  }
  unsafeUpdateNodeCache(A, j, e) {
    A = A.draftOrCurrent();
    let r = WA(A) && !A.cache.overlayVisible;
    j &&= D.isOn("visibilityFromSandbox") ? this.sandboxVisibility.getVisible(A.id) : A.isVisible();
    let o = j && !r;
    ((A.cache.visible = o),
      (e ||= pA(A)),
      (A.cache.insideVariant = e),
      A.children?.forEach((i) => this.unsafeUpdateNodeCache(i, o, e)));
  }
  get remoteTreeVersion() {
    return this.timeline.remoteTreeVersion;
  }
  debugComputeLazyIndexes() {
    let A = {
      codeComponents: this.codeComponents,
      richTextNodes: this.richTextNodes,
      shaderNodes: this.shaderNodes,
      formContainerNodeIds: this.formContainerNodeIds,
      withDataIdentifierNodeIds: this.withDataIdentifierNodeIds,
      localSmartComponentNameByIdentifier: this.localSmartComponentNameByIdentifier,
      localLayoutTemplatesNameByIdentifier: this.localLayoutTemplatesNameByIdentifier,
      activeScopeCodeComponentArrows: this.activeScopeCodeComponentArrows,
      activeScopeLinkedNavigationNodes: this.activeScopeLinkedNavigationNodes,
      activeScopeLinkedVariantNodes: this.activeScopeLinkedVariantNodes,
      activeScopeLinkedGestureNodes: this.activeScopeLinkedGestureNodes,
    };
  }
  get branchId() {
    return this.mode === "crdt" ? this.crdtTimeline.branches.activeBranchId : ej;
  }
  isOnMainBranch() {
    return this.branchId === ej;
  }
};
((Zj = cj(null)), (j6 = new WeakMap()), EA(Zj, 4, "tree", V7, OA, j6), $j(Zj, OA));
function U7(t, A) {
  if (!t || t.length === 0) return A?.placeholder ?? "";
  if (t.length === 1) return t[0] && t[0] !== "" ? t[0] : (A?.placeholder ?? "");
  let j = [...t],
    e = "";
  if (A?.max && A.max < t.length) {
    let r = t.length - A.max;
    ((j = j.slice(0, A.max)), (e = `${r} ${r > 1 ? "others" : "other"}`));
  } else e = j.pop() ?? "";
  return j.length > 1 ? `${j.join(", ")}, and ${e}` : `${j.join(", ")} and ${e}`;
}
function nj(t) {
  let A = G(t);
  if (!(!A || !J(A) || !q(A))) return `${A.moduleId}:${A.exportSpecifier}`;
}
function X7(t) {
  let A = new Map();
  for (let j of t) {
    let e = nj(j);
    e && A.set(e, j);
  }
  return A;
}
function Q7(t, A) {
  let j = t.getRawControlProps();
  for (let e in j) {
    let r = j[e]?.type,
      o = j[e]?.value;
    if (r !== "vectorsetitem" || !m(o) || !J(o)) continue;
    let i = nj(o);
    if (!i || !A.has(i)) continue;
    let n = A.get(i);
    n && t.setControlProp(e, { type: "vectorsetitem", value: n });
  }
}
function z7(t, A) {
  for (let j of A) {
    Q7(j, t);
    let e = nj(j.codeComponentIdentifier);
    if (!e || !t.has(e)) continue;
    let r = t.get(e);
    r && j.set({ codeComponentIdentifier: r });
  }
}
function W7(t, A) {
  let j = xt(A);
  if (j) {
    d(j.isLoaded(), "CMS Should always be loaded.");
    for (let { node: e, skipChildren: r } of j.walkWithSkipChildren()) {
      if (Y(e)) {
        if (!e.variables.some((n) => n.type === "vectorsetitem")) {
          r();
          continue;
        }
        Y7(e, t);
      }
      if (!P(e)) continue;
      let o = e.getControlProps();
      for (let i in o) {
        let n = o[i]?.value;
        if (o[i]?.type !== "vectorsetitem" || !m(n) || !J(n)) continue;
        let a = nj(n);
        if (!a || !t.has(a)) continue;
        let k = t.get(a);
        k && e.setControlProp(i, { ...o[i], value: k });
      }
    }
  }
}
function Y7(t, A) {
  let j = !1,
    e = t.variables.map((r) => {
      if (r.type !== "vectorsetitem") return r;
      let { initialValue: o } = r,
        i = o.identifier;
      if (!i) return r;
      let n = nj(i);
      if (!n || !A.has(n)) return r;
      let s = A.get(n);
      return s ? ((j = !0), { ...r, initialValue: { ...o, identifier: s } }) : r;
    });
  j && t.set({ variables: e });
}
function Z7(t, A) {
  for (let j of A.root.children) {
    if (aA(j)) {
      Q7(j, t);
      continue;
    }
    (jA(j) || DA(j)) && Y7(j, t);
  }
}
var O = BA("modules-store"),
  Ao = new Set(["codeFile", "canvasComponent", "screen", "layoutTemplate", "vector"]),
  bn = 500;
function Sn(t) {
  return "sourceContent" in t && m(t.sourceContent);
}
function Ae(t) {
  let { module: A, exportSpecifier: j } = t,
    { id: e, saveId: r, files: o } = A;
  return (d(o.module, "module file must exist for a saved module"), gA(e, r, o.module, j));
}
var jo, eo, to, ro, oo, w, e6, t6, r6, o6, n6, JA, ij, aj, mA, MA, RA;
((oo = [uA()]), (ro = [uA()]), (to = [uA()]), (eo = [uA()]), (jo = [uA()]));
var GA = class {
  constructor(A, j, e, r, o, i, n, s, a, k, l) {
    this.scheduler = A;
    this.componentLoader = j;
    this.treeStore = e;
    this.loadedExternalModulesStore = r;
    this.chromeStore = o;
    this.modulesAPIServicePromise = i;
    this.makeDocumentReadOnly = n;
    this.insertTemporaryImportMap = s;
    this.loadLocalModules = a;
    this.loadAllLocalModules = k;
    this.abortSignal = l;
    (V(this, e6, H(w, 8, this)), H(w, 11, this));
    (V(this, t6, H(w, 12, this, new Map())), H(w, 15, this));
    (V(this, r6, H(w, 16, this, new Map())), H(w, 19, this));
    (V(this, o6, H(w, 20, this, new Map())), H(w, 23, this));
    (V(this, n6, H(w, 24, this, 0)), H(w, 27, this));
    V(this, JA, new Map());
    u(this, "moduleDepsGraph", {});
    u(this, "modulesStorage");
    u(this, "moduleUpdatesEmitter", new re());
    u(this, "fastRefreshModulesEmitter", new re());
    V(this, ij);
    u(this, "prepareForPersistence", async (A, j) => {
      if (D.isOn("crdtModuleControls") && A === "codeFile") {
        if (j.length > 0) {
          let e = new Set(j.map((r) => HA(r, "default").value));
          await this.loadLocalModules(e, "current");
        }
        await $r(this.componentLoader, this, "code-file-persistence");
      }
    });
    V(this, aj, new Map());
    V(this, mA, new Map());
    V(this, MA, !1);
    V(this, RA, []);
    u(
      this,
      "debugModuleStoreAndComponentLoaderRevisionsMatch",
      () => this.revision === this.componentLoader.modulesRevision
    );
    u(this, "checkForExternalModuleUpdates", async () => {
      if (this.treeStore.tree.isViewOnly) return;
      let A = kA.get(this.treeStore.tree);
      if (!A?.children?.length) return;
      let j = [],
        e = new Map();
      A.children.forEach((n) => {
        let s = G(n.codeComponentIdentifier);
        if (
          (d(
            s?.kind === "externalModuleExport",
            "expected ExternalModuleNode identifier to be of kind externalModuleExport"
          ),
          n.type === "vector")
        ) {
          let a = n.annotation("framerVector");
          if (!qA(a)) return;
          let k = a.set.moduleId,
            l = e.get(k) ?? [];
          (l.push(s), e.set(k, l));
        } else j.push(s);
      });
      for (let [n, s] of e) {
        let a = this.treeStore.tree.get(n);
        if (!U(a)) continue;
        let k = a.annotation("framerVectorSet");
        Aj(k) && (wt(k) || j.push(...s));
      }
      let r = j.map(({ moduleId: n, saveId: s }) => ({ moduleId: n, saveId: s }));
      if (!r.length) return;
      let o = [],
        i = [];
      for (let n = 0; n < r.length; n++) {
        let s = r[n];
        if (
          (d(s, "query is expected to be defined."), o.push(s), o.length < bn && n < r.length - 1)
        )
          continue;
        let { modules: a } = await this.lookUpModules(o, { includeStatus: !0 });
        (i.push(...a), (o = []));
      }
      this.treeStore.tree.isViewOnly ||
        this.updateExternalModuleNodes(
          i.map((n, s) => {
            let a = j[s];
            return (
              d(a, "External module missing a matching identifier"),
              { module: n, exportSpecifier: a.exportSpecifier }
            );
          }),
          { onTreeUpdate() {} }
        ).catch(z);
    });
    u(this, "updatesPollId");
    u(
      this,
      "compileFastRefreshModule",
      async ({ localId: A, name: j, source: e, includeSourceMap: r = !0 }) => {
        let { type: o, name: i } = d6(A);
        if (!Ao.has(o)) return;
        let n = o === "codeFile" ? yA(j).source : i,
          s = o === "codeFile" ? It(j)[1] : i,
          {
            code: a,
            imports: k,
            sourceMap: l,
          } = await c7({ localId: A, source: e, name: n, includeSourceMap: r }),
          c = {
            kind: "fast-refresh",
            localId: A,
            type: o,
            name: s,
            sourceContent: e,
            moduleContent: a,
            relativeImports: k.relative,
            sourceMapContent: l,
            binaryAssetContents: void 0,
            submoduleContents: {},
            update: 0,
          },
          [$, B] = Ur(this.fastRefreshModules, (E) => {
            E.set(j, c);
          });
        ((this.fastRefreshModules = $),
          this.fastRefreshModulesEmitter.emit({ patches: B, initialized: !0 }));
      }
    );
    u(
      this,
      "compile",
      ({
        localId: A,
        name: j,
        source: e,
        includeSourceMap: r = !0,
        type: o,
        telemetrySession: i,
      }) => {
        let n = $7({ name: j, source: e, includeSourceMap: r, type: o, telemetrySession: i });
        return (
          this.fastRefreshEnabled &&
            A &&
            this.compileFastRefreshModule({
              localId: A,
              name: j,
              source: e,
              includeSourceMap: r,
            }).catch(z),
          n
        );
      }
    );
    ((this.modulesStorage = new t7(
      () =>
        this.modulesAPIServicePromise.catch((c) => {
          throw (O.error(c, { context: "Failed to discover ModulesAPI service:" }), c);
        }),
      this.compile,
      (c, $) => this.scheduler.process(c, $),
      (c, $) => this.scheduler.processWhenReady(c, $),
      () => this.scheduler.markCurrentCommitHasUserModuleEdit(),
      (c) => this.scheduler.runWhenIdle(c),
      (c, $, B) => this.scheduler.scheduleAgentChanges(c, $, B),
      e,
      () => this.getActiveScope(),
      () => this.makeDocumentReadOnly(),
      this.abortSignal,
      this.prepareForPersistence,
      this.propertyControlsSnapshotForCurrentRevision.bind(this)
    )),
      (this.fastRefreshModulesEmitter.onNewStream = (c) => (
        O.debug("onNewStream - fast refresh", c),
        { latest: this.getAllFastRefreshModulesAsUpdateEvent() }
      )),
      (this.moduleUpdatesEmitter.onNewStream = (c) => (
        O.debug("onNewStream - module updates", c),
        { latest: this.getAllModulesAsUpdateEvent() }
      )),
      Kr(
        {
          check: () =>
            hA.isTest || this.treeStore.tree.isViewOnly || !D.isOn("importMapPruning")
              ? !1
              : C(this, MA) || C(this, RA).length > 0,
          effect: () => {
            O.debug("Showing Unsaved Changes dialog (import map queue processing)");
          },
        },
        this.abortSignal
      ),
      this.modulesStorage.subscribe((c) => {
        let $ = c.dependenciesModule?.importMapContent,
          B = this.dependenciesModule?.importMapContent,
          E = mt(B, $),
          p = !0;
        D.isOn("importMapPruning") && (p = E);
        let R = !1;
        if ($ && $ !== B) {
          let S = Gt($);
          for (let y in S.imports ?? {}) C(this, JA).set(y, "success");
          R = c.metadata.multiplayerChange || E;
        }
        ($ !== B &&
          (E
            ? O.info("Import map resolutions changed, re-evaluating all modules")
            : p
              ? O.info("Import map updated, re-evaluating all modules")
              : O.info(
                  "Import map updated without existing resolution changes, skipping blanket re-eval"
                )),
          R && c.metadata.multiplayerChange,
          (this.dependenciesModule = c.dependenciesModule),
          (this.localModules = c.modules));
        let g = this.moduleDepsGraph;
        this.moduleDepsGraph = c.depsGraph;
        let F = c.metadata.patches,
          h = new Set(),
          _ = [],
          b = [],
          T;
        for (let S of F) {
          let y = S.path[0];
          if ((d(typeof y == "string"), y === B6)) {
            if (((T = S), p)) for (let I of c.modules.keys()) h.add(I);
            break;
          }
          S.op === "remove" && S.path.length === 1 ? b.push(y) : _.push(y);
        }
        let x = new Set();
        if (!T) {
          let S = this.getActiveScope(),
            y = Ke(S);
          O.debug("Calculated visible code component module typeSlashNames", y);
          for (let I of _) qe(c.depsGraph, I, h, y, x);
          for (let I of b) qe(g, I, h, y, x);
          (_.forEach((I) => {
            h.delete(I);
          }),
            b.forEach((I) => {
              (h.delete(I), x.delete(I));
            }));
        }
        O.debug(
          "prioritized evaluation sending to sandbox",
          { dependentModules: h, visibleDependentModules: x },
          "highprio size",
          x.size,
          "dep size",
          h.size
        );
        let f = {
          patches: F,
          dependentModules: Array.from(h),
          prioritizedModules: Array.from(x),
          revision: F.length === 0 ? this.revision : ++this.revision,
          initialized: c.initialized,
          modulesReloading: c.modulesReloading,
        };
        if (
          (this.moduleUpdatesEmitter.emit(f),
          this.fastRefreshEnabled &&
            T &&
            this.fastRefreshModulesEmitter.emit({ patches: [T], initialized: c.initialized }),
          this.fastRefreshEnabled && !T)
        )
          for (let S of _) {
            let y = this.fastRefreshModules.get(S);
            if (!y) continue;
            let I = this.localModules.get(S);
            I && I.sourceContent !== y.sourceContent && this.createFastRefreshModuleFromModule(I);
          }
      }));
  }
  scheduler;
  componentLoader;
  treeStore;
  loadedExternalModulesStore;
  chromeStore;
  modulesAPIServicePromise;
  makeDocumentReadOnly;
  insertTemporaryImportMap;
  loadLocalModules;
  loadAllLocalModules;
  abortSignal;
  propertyControlsSnapshotForCurrentRevision() {
    return D.isOn("crdtModuleControls")
      ? this.debugModuleStoreAndComponentLoaderRevisionsMatch()
        ? { revision: this.revision, controlsByLocalId: ze(this.componentLoader.getAllEntities()) }
        : { revision: -1, controlsByLocalId: new Map() }
      : { revision: this.revision, controlsByLocalId: new Map() };
  }
  localCodeModuleIdsMissingPropertyControls() {
    let A = [];
    for (let j of Sj.getModuleNodes(this.treeStore.tree))
      j.save.type === "codeFile" && j.save.propertyControls === void 0 && A.push(j.id);
    return A;
  }
  async backfillMissingCrdtModuleControls() {
    if (!D.isOn("crdtModuleControls") || this.treeStore.tree.isViewOnly) return;
    let A = this.localCodeModuleIdsMissingPropertyControls(),
      j = x7(this.treeStore.tree);
    if (A.length === 0 && !j) return;
    if (A.length > 0) {
      let s = new Set(A.map((a) => HA(a, "default").value));
      await this.loadLocalModules(s, "eager");
    }
    let e = N7(this.treeStore.tree);
    await Promise.allSettled(
      e.map((s) => this.loadedExternalModulesStore.ensureModuleEvaluatedInSandbox(s))
    );
    let r = this.loadedExternalModulesStore.usesNoopExternalModuleEvaluation?.() === !0,
      o = [...this.componentLoader.getAllEntities()],
      i = ze(o),
      n = We(o);
    await this.scheduler.processWhenReadyAsync(() => {
      if (!this.treeStore.tree.isViewOnly) {
        for (let s of Sj.getModuleNodes(this.treeStore.tree)) {
          if (s.save.type !== "codeFile" || s.save.propertyControls !== void 0) continue;
          let a = i.get(s.id) ?? null;
          s.set({ save: { ...s.save, propertyControls: Nj(a) } });
        }
        for (let s of kA.getModuleNodes(this.treeStore.tree)) {
          let a = Qj(s);
          if (!a || s.propertyControlsBySaveId?.[a] !== void 0) continue;
          let k = Ye(n, s.id, a);
          (k === void 0 && !r) || s.set({ propertyControlsBySaveId: Ze(s, a, k ?? null) });
        }
      }
    });
  }
  getActiveScope() {
    if (this.chromeStore.mainView === 5) return;
    let A = this.treeStore.tree.getNode(C(this, ij));
    if (A) return (d(A?.loaded, "Active scope node should be loaded"), A.loaded);
  }
  setActiveScope(A) {
    dj(this, ij, A);
  }
  getLatestDependencyGraph() {
    return this.moduleDepsGraph;
  }
  initialize() {
    return this.initialized
      ? this.modulesStorage.refresh()
      : (this.setupPollingExternalModulesUpdates(), this.modulesStorage.initialize());
  }
  get initialized() {
    return this.modulesStorage.initialized;
  }
  whenInitialized() {
    return this.modulesStorage.whenInitialized();
  }
  whenIdle() {
    return this.modulesStorage.whenIdle();
  }
  startDivergenceReporter() {
    this.modulesStorage.startDivergenceReporter();
  }
  sampleDivergencesNow() {
    this.modulesStorage.sampleDivergencesNow();
  }
  getImportMapTraceCacheText() {
    return this.modulesStorage.getImportMapTraceCacheText();
  }
  refresh() {
    return this.modulesStorage.refresh();
  }
  deleteModules(A) {
    return this.modulesStorage.delete(A);
  }
  softDeleteModulesInBackend(A) {
    return this.modulesStorage.softDeleteModulesInBackend(A);
  }
  restoreModulesInBackend(A) {
    return this.modulesStorage.restoreModulesInBackend(A);
  }
  restoreModule(A, j) {
    return this.modulesStorage.restore(A, j);
  }
  upsertBatch(A) {
    return this.modulesStorage.upsertBatch(A);
  }
  promoteModuleSaves(A) {
    return this.modulesStorage.promoteModuleSaves(A);
  }
  forType(A) {
    return new e7(this.modulesStorage, A, this.debugModuleStoreAndComponentLoaderRevisionsMatch);
  }
  getNpmDependencyInstallStatus(A) {
    return C(this, JA).get(Rj(A)) ?? "unknown";
  }
  async addNpmDependencies(A) {
    let e = typeof this.dependenciesModule > "u" && !u7(),
      r = await qj(this.modulesStorage.addNpmDependencies(A)),
      o = Array.isArray(A) ? A : [A],
      i = o.filter(({ target: a }) => !Re(a));
    for (let a of i) C(this, JA).set(Rj(a), r.ok ? "success" : "failure");
    if (!r.ok) {
      let a = r.error instanceof Error ? r.error.message : String(r.error);
      for (let k of i) {
        let l = Rj(k);
        L("npm_dependency_add_fail", { name: l, reason: a });
      }
      throw (
        i.length > 0 &&
          O.reportError(r.error, {
            context: "Failed to add npm dependencies",
            descriptors: i.map(({ target: k }) => k),
          }),
        r.error
      );
    }
    e && G7();
    let n = Object.keys(r.value.dependenciesMap.dependencies),
      s = o.map(({ target: a }) => a).filter((a) => !n.includes(a) && Re(a));
    if (s.length > 0) {
      let a = U7(s, { max: 5, placeholder: "package" });
      X({
        type: "add",
        variant: "warning",
        primaryText: a,
        secondaryText: `${s.length === 1 ? "is" : "are"} incompatible.`,
        duration: 5e3,
      });
    }
    return r.value;
  }
  changeScope(A) {
    (this.setActiveScope(A.id), this.modulesStorage.changeScope(A));
  }
  async uninstallBlockedNpmDependencies() {
    return { importMap: await this.modulesStorage.uninstallBlockedNpmDependencies() };
  }
  async unsafeUpgradeDependency(A, j, e) {
    return { importMap: await this.modulesStorage.unsafeUpgradeDependency(A, j, e) };
  }
  async clearNpmDependencies() {
    return this.modulesStorage.clearNpmDependencies();
  }
  async removeBlockedNpmDependencies() {
    return { dependenciesMap: await this.modulesStorage.removeBlockedNpmDependencies() };
  }
  async pruneProjectImportMapFromEntryPoints(A = !1) {
    let j = this.getEntryPointModuleNames();
    return {
      importMap: await this.modulesStorage.pruneProjectImportMapFromEntryPoints(j, { dryRun: A }),
    };
  }
  getEntryPointModuleNames() {
    let A = [];
    for (let j of this.localModules.values()) {
      if (
        j.kind === "fast-refresh" ||
        (j.type !== "codeFile" && j.type !== "canvasComponent" && j.type !== "screen")
      )
        continue;
      let e = j.files?.module;
      if (!e) continue;
      let r = ee(j.localId, e);
      A.push(r);
    }
    for (let j of kA.getModuleNodes(this.treeStore.tree)) {
      if (j.type !== "codeFile" && j.type !== "canvasComponent") continue;
      let e = G(j.codeComponentIdentifier);
      q(e) && A.push(e.importSpecifier);
    }
    return [...new Set(A)];
  }
  async updateSources(A) {
    return this.modulesStorage.updateSources(A);
  }
  async updateDependenciesSource(A, j, e) {
    await this.modulesStorage.updateDependenciesSource(A, j, e);
  }
  getDependentsOfModule(A) {
    return this.modulesStorage.getDependentsOfModule(A);
  }
  subscribeToModulesStorage(A) {
    return this.modulesStorage.subscribe(A);
  }
  async getModuleDependencies(A) {
    let j = await this.modulesAPIServicePromise;
    return (
      d(j, "Modules API service not initialized correctly"),
      await j.getModuleDependencies(A)
    );
  }
  getModuleEntryByLocalId(A) {
    for (let j of this.localModules.values()) if (j.localId === A) return j;
  }
  getModuleEntryByUniqueName(A) {
    for (let j of this.localModules.values()) if (j.name === A) return j;
  }
  getPersistedDependenciesModule() {
    return this.modulesStorage.getPersistedDependenciesModule();
  }
  getPersistedModuleByGlobalId(A) {
    return this.modulesStorage.getPersistedModuleByGlobalId(A);
  }
  getPersistedModuleByLocalId(A) {
    return this.modulesStorage.getPersistedModuleByLocalId(A);
  }
  getPersistedModuleByLocalIdentifier(A) {
    let j = G(A);
    if (K(j)) return this.getPersistedModuleByLocalId(j.localId);
  }
  tryConvertExternalCodeComponentIdentifierToLocal(A) {
    let j = G(A);
    if (j?.kind !== "externalModuleExport") return;
    let e = this.getPersistedModuleByGlobalId(j.moduleId);
    if (e) return HA(e.localId, j.exportSpecifier).value;
  }
  async listNamespaces() {
    let A = await this.modulesAPIServicePromise;
    return (d(A, "Modules API service not initialized correctly"), await A.listNamespaces());
  }
  async createNamespace(A, j) {
    let e = await this.modulesAPIServicePromise;
    return (
      d(e, "Modules API service not initialized correctly"),
      await e.createNamespace({ ownerId: A, ownerType: j })
    );
  }
  async lookUpModule(A, j) {
    let { modules: e, dependencies: r } = await this.lookUpModules([A], j),
      o = e[0];
    d(o, () => `Module does not exist, looking up: ${JSON.stringify(A)}`);
    let i = { module: o };
    return (r && (i.dependencies = r[o.ownerType]?.[o.ownerId]), i);
  }
  async lookUpModuleURL(A) {
    let j = new URL(A),
      e = j.hash.substring(1).split(","),
      r = e[0] || "default",
      o = e.some((l, c) => c > 0 && l === "unlink"),
      { module: i } = await this.lookUpModule({ url: j.href }),
      n = i.id,
      s = this.getPersistedModuleByGlobalId(n);
    d(i.files.module, "Module file must be defined");
    let a = gA(n, i.saveId, i.files.module, r),
      k = s ? HA(s.localId, r) : a;
    return { module: i, moduleIdentifier: k, externalIdentifier: a, insertUnlinked: o };
  }
  async getSourceContentForModuleSave(A) {
    let { module: j } = await this.lookUpModule(A);
    if (Sn(j)) return j.sourceContent;
    let e = j.baseURL + j.files.source,
      r = await qj(fetch(e));
    if (!r.ok)
      throw (
        O.reportError(r.error, {
          context: "Failed to fetch source content for module save",
          moduleId: A.moduleId,
          saveId: A.saveId,
          sourceURL: e,
        }),
        r.error
      );
    if (!r.value.ok) {
      let i = new Error(
        `Failed to fetch source content for module save ${A.moduleId}@${A.saveId}: ${r.value.status} ${r.value.statusText}`
      );
      throw (
        O.reportError(i, {
          context: "Failed to fetch source content for module save",
          moduleId: A.moduleId,
          saveId: A.saveId,
          sourceURL: e,
          httpStatus: r.value.status,
        }),
        i
      );
    }
    let o = await qj(r.value.text());
    if (!o.ok)
      throw (
        O.reportError(o.error, {
          context: "Failed to read source content for module save",
          moduleId: A.moduleId,
          saveId: A.saveId,
          sourceURL: e,
        }),
        o.error
      );
    return o.value;
  }
  async lookUpModules(A, j) {
    let e = await this.modulesAPIServicePromise;
    if (!e)
      return (
        O.error("Modules API service not initialized correctly"),
        { modules: [], dependencies: {} }
      );
    let { data: r, dependencies: o } = await e.lookUpModules({ queries: A, ...j }),
      i = new Map(this.externalModulesDynamicInfo);
    for (let s of r) {
      let a = s.id,
        { localId: k, title: l, description: c, lastPublish: $, status: B } = s;
      i.set(a, {
        localId: k,
        title: l,
        description: c,
        lastPublish: $,
        status: B ?? i.get(a)?.status,
      });
    }
    this.externalModulesDynamicInfo = i;
    let n = { modules: r };
    return (o && (n.dependencies = o), n);
  }
  trackExternalComponentInsert(A, j) {
    let e = z6();
    for (let r of A) s7({ insertId: e, insertType: j, codeComponentId: r });
  }
  async installExternalModulesDependencies(A) {
    let j = [],
      e = [];
    for (let r of A) {
      let o = C(this, aj).get(r);
      o ? e.push(o) : j.push(r);
    }
    if (j.length > 0) {
      let r = this.installDependenciesForModuleURLs(j);
      for (let o of j) C(this, aj).set(o, r);
      e.push(r);
    }
    return Promise.allSettled(e).then((r) => {
      for (let o of r)
        o.status !== "fulfilled" && O.error("Failed to install external module", o.reason);
    });
  }
  isProcessingModules() {
    return this.modulesStorage.isProcessing();
  }
  hasLocalCodeFileChanges() {
    return this.modulesStorage.hasLocalCodeFileChanges();
  }
  isReadOnly() {
    return this.modulesStorage.isReadOnly();
  }
  resetModuleStateAndPermissions(A) {
    this.modulesStorage.resetModuleStateAndPermissions(A);
  }
  getTransientSave(A) {
    return this.modulesStorage.getTransientSave(A);
  }
  createFastRefreshModuleFromModule(A) {
    if (Ao.has(A.type)) {
      if (A.kind === "server") {
        let j = this.getPersistedModuleByLocalId(A.localId);
        (d(j?.kind === "server", "Only server modules should have no sourceContent"),
          this.modulesStorage.createLocalModuleFromModule(j).catch(z));
        return;
      }
      this.compileFastRefreshModule({
        name: ft(A),
        localId: A.localId,
        source: A.sourceContent,
      }).catch(z);
    }
  }
  getModuleWithTypeSlashName(A) {
    return this.modulesStorage.getModuleWithTypeSlashName(A);
  }
  waitForModulesToSave() {
    return this.modulesStorage.waitForModulesToSave();
  }
  async insertTemporaryImportMapForExternalModules(A) {
    let j = { imports: {} };
    for (let { dependencies: e, module: r } of A) {
      let o = e?.importMap;
      if (!(!o || Fj(o))) {
        if ((d(r.files.module, "Module must have a module file"), o.scopes)) {
          j.scopes ??= {};
          for (let [i, n] of Object.entries(o.scopes))
            j.scopes[i] = Object.assign({}, n, j.scopes[i]);
        }
        Object.keys(o.imports).length > 0 &&
          ((j.scopes ??= {}),
          (j.scopes[r.baseURL] = Object.assign({}, o.imports, j.scopes[r.baseURL])));
      }
    }
    Fj(j) || (await this.insertTemporaryImportMap(j));
  }
  async installDependenciesForExternalModules(A) {
    let j,
      e,
      r = [];
    for (let { dependencies: o, module: i } of A)
      if (o?.importMap)
        if (D.isOn("importMapPruning")) {
          (d(i.files.module, "Module must have a module file"),
            r.push(`${i.baseURL}${i.files.module}`));
          let { importMap: n } = $t(o.importMap, [i.baseURL], Bt);
          j = fj(j ?? { imports: {} }, n);
        } else {
          let n = pt(o.importMap, [i.baseURL]),
            s = n.importMap,
            a =
              n.didNormalize && o.dependencies ? ut(o.dependencies, s) : (o.dependencies ?? ct(s)),
            k = {
              dependencies: a.dependencies,
              ...(a.resolutions ? { resolutions: a.resolutions } : {}),
            };
          ((e = Rt(e ?? { dependencies: {} }, k)), (j = fj(j ?? { imports: {} }, s)));
        }
    if (!(!j || Fj(j))) {
      if (D.isOn("importMapPruning") && r.length > 0) {
        this.queueImportMapUpdate({ importMap: j, entryPoints: r }).catch(z);
        return;
      }
      !D.isOn("importMapPruning") && e && (await this.modulesStorage.extendCurrentImportMap(j, e));
    }
  }
  installDependenciesForModuleURLs(A) {
    return (
      O.info(
        `\u{1F986} Installing external modules:
	`,
        A.join(`
	`)
      ),
      this.lookUpModules(
        A.map((j) => ({ url: j })),
        { includeDependencies: !0 }
      ).then(async ({ modules: j, dependencies: e = {} }) => {
        let r = j.map((o) => ({ module: o, dependencies: e[o.ownerType]?.[o.ownerId] }));
        (await this.insertTemporaryImportMapForExternalModules(r),
          await this.installDependenciesForExternalModules(r));
      })
    );
  }
  async addOrUpdateExternalVectors(A) {
    await this.modulesStorage.whenInitialized();
    let { modules: j } = await this.lookUpModules(
        A.filter((o) => {
          let i = this.treeStore.tree.getNodeWithTrait(o.moduleId, U);
          return i ? G(i.codeComponentIdentifier).saveId !== o.saveId : !0;
        }),
        { includeDependencies: !1, includeStatus: !0 }
      ),
      e = new Map(),
      r = "default";
    for (let o of j) {
      let i = Ae({ module: o, exportSpecifier: r }),
        n = this.treeStore.tree.getNodeWithTrait(o.id, U);
      (J(n?.codeComponentIdentifier) && G(n.codeComponentIdentifier).saveId === o.saveId) ||
        e.set(i.value, { module: o, exportSpecifier: r });
    }
    (await Promise.all(
      Array.from(e.keys(), (o) => {
        let i = G(o);
        return (
          d(i, "Identifier must be a valid module identifier"),
          this.loadedExternalModulesStore.ensureModuleEvaluatedInSandbox(i).catch((n) => {
            O.error(`Failed to evaluate external module "${o}"`, n);
          })
        );
      })
    ),
      this.treeStore.getDataTree() ||
        (await this.scheduler.runWithFullyLoadedTreeAsync({
          name: "update_external_module_version",
          runInBackground: !0,
        })),
      await this.updateExternalModuleNodes(Array.from(e.values()), {
        onTreeUpdate: () => {
          let o = X7(e.keys()),
            i = this.treeStore.getDataTreeOrPartialTree(),
            n = i.query().whereClass(jj).iterate();
          (z7(o, n), W7(o, i), Z7(o, i));
        },
        updateSave: !0,
      }));
  }
  async addOrUpdateVectorSetAndVectors(A) {
    await this.addOrUpdateExternalVectors([A]);
    let e = this.treeStore.tree.getNodeWithTrait(A.moduleId, U)?.annotation("framerVectorSet");
    Aj(e) &&
      (await this.addOrUpdateExternalVectors(
        e.items.map((r) => ({ moduleId: r.moduleId, saveId: r.saveId ?? "" }))
      ));
  }
  async addExternalModulesToProject(A, j) {
    let e = A.map((o) => (typeof o == "string" ? G(o) : o)),
      r = await this.preloadExternalModules(e);
    return this.updateExternalModuleNodes(r, j);
  }
  usesNoopExternalModuleEvaluation() {
    return this.loadedExternalModulesStore.usesNoopExternalModuleEvaluation?.() === !0;
  }
  async preloadExternalModules(A) {
    if (A.length === 0) return [];
    await this.modulesStorage.whenInitialized();
    let j = new Map();
    for (let s of A) j.set(s.bareValue, s);
    if (this.loadedExternalModulesStore.usesNoopExternalModuleEvaluation?.() === !0)
      return (
        await Promise.all(
          Array.from(j.values(), (s) =>
            this.loadedExternalModulesStore.ensureModuleEvaluatedInSandbox(s)
          )
        ),
        []
      );
    let e = [],
      r = [],
      o = [];
    for (let [s, a] of j) {
      let { moduleId: k, saveId: l } = a;
      if (!!this.modulesStorage.getPersistedModuleByGlobalId(k)) {
        O.reportError("Attempted to treat local module as external module", { identifier: a });
        continue;
      }
      let $ = this.treeStore.tree.getNode(k);
      if (
        $ &&
        G($.codeComponentIdentifier).saveId === l &&
        !this.loadedExternalModulesStore.hasModuleForIdentifier(a)
      ) {
        r.push(a);
        continue;
      }
      let B = C(this, mA).get(s);
      if (B) {
        o.push({ identifier: a, preloadPromise: B });
        continue;
      }
      (C(this, mA).set(s, new te()), e.push(a));
    }
    let i = new Map();
    for (let { identifier: s, preloadPromise: a } of o)
      try {
        let k = await a;
        i.set(s.value, k);
      } catch {
        (C(this, mA).set(s.bareValue, new te()), e.push(s));
      }
    if (e.length)
      try {
        let s = Array.from(e.values(), ({ moduleId: c, saveId: $ }) => ({
            moduleId: c,
            saveId: $,
          })),
          { modules: a, dependencies: k } = await this.lookUpModules(s, {
            includeDependencies: !0,
            includeStatus: !0,
          }),
          l = 0;
        for (let c of e) {
          let $ = a[l++];
          (d($, "Module must be defined"),
            i.set(c.value, {
              module: $,
              dependencies: k?.[$.ownerType]?.[$.ownerId],
              exportSpecifier: c.exportSpecifier,
            }),
            r.push(c));
        }
      } catch (s) {
        O.reportError(s);
        for (let a of e) C(this, mA).get(a.bareValue)?.reject(s);
        throw Error(`Failed to preload modules: ${s}`);
      }
    let n = [];
    for (let s of r) {
      let a = i.get(s.value);
      a && n.push(a);
    }
    (await this.insertTemporaryImportMapForExternalModules(n),
      await Promise.all(
        Array.from(r.values(), (s) =>
          this.loadedExternalModulesStore.ensureModuleEvaluatedInSandbox(s).catch((a) => {
            O.error(`Failed to evaluate external module "${s.value}"`, a);
          })
        )
      ));
    for (let s of e) {
      let a = i.get(s.value);
      d(a, () => `Expected to have module information for: ${s.value}`);
      let k = C(this, mA).get(s.bareValue);
      (d(k, () => `Expected to find a preload promise for: ${s.bareValue}`), k.resolve(a));
    }
    return [...i.values()];
  }
  queueImportMapUpdate(A) {
    return new Promise((j, e) => {
      (C(this, RA).push({ externalDeps: A, resolve: j, reject: e }),
        this.processImportMapUpdateQueue());
    });
  }
  async processImportMapUpdateQueue() {
    if (C(this, MA)) return;
    dj(this, MA, !0);
    let A = "import-map-prune",
      j = !1;
    X({
      type: "add",
      key: A,
      text: "Optimizing dependencies\u2026",
      variant: "progress",
      duration: 1 / 0,
      showCloseButton: "never",
    });
    try {
      for (; C(this, RA).length > 0; ) {
        let e = C(this, RA).splice(0, C(this, RA).length);
        try {
          let r = [],
            o = { imports: {} };
          for (let { externalDeps: s } of e) {
            let a = await this.modulesStorage.scopeExternalImportMap(s.entryPoints, s.importMap);
            ((o = fj(o, a)), r.push(...s.entryPoints));
          }
          let i = [...new Set([...this.getEntryPointModuleNames(), ...r])],
            n = await this.modulesStorage.pruneProjectImportMapFromEntryPoints(i, {
              additionalImportMap: o,
            });
          for (let { resolve: s } of e) s(n);
        } catch (r) {
          j = !0;
          for (let { reject: o } of e) o(r);
        }
      }
    } finally {
      (j
        ? X({
            type: "add",
            key: A,
            text: "Failed to optimize dependencies.",
            variant: "error",
            duration: 3e3,
          })
        : X({ type: "remove", key: A }),
        dj(this, MA, !1));
    }
  }
  async updateExternalModuleNodes(
    A,
    { onTreeUpdate: j, updateSave: e = !1, scheduleTreeUpdate: r }
  ) {
    (await this.insertTemporaryImportMapForExternalModules(A),
      await Promise.all(
        A.map((n) => this.loadedExternalModulesStore.ensureModuleEvaluatedInSandbox(Ae(n)))
      ));
    let i = await (r ?? ((n) => this.scheduler.processWhenReadyAsync(n)))(() => {
      if (this.treeStore.tree.isViewOnly) throw Error("unable to edit");
      let n = new Set();
      for (let a of A) n.add(a.module.id);
      let s = D.isOn("crdtModuleControls") ? We(this.componentLoader.getAllEntities(), n) : void 0;
      for (let a of A) {
        let { id: k } = a.module,
          l = this.treeStore.tree.getNode(k);
        if (l && !e) {
          let c = Ae(a),
            $ = this.externalModulesDynamicInfo.get(c.moduleId)?.status ?? a.module.status;
          l.set({ group: $?.group, updateSaveId: $?.updateSaveId });
        } else {
          let { type: c, metadata: $, ownerId: B, ownerType: E } = a.module,
            p = Ae(a),
            { title: R, status: g } = this.externalModulesDynamicInfo.get(p.moduleId) ?? a.module,
            F = kA.get(this.treeStore.tree);
          (F || ((F = new kA()), this.treeStore.tree.insertNode(F, this.treeStore.tree.root.id)),
            d(uj(F)));
          let h = $.pluginId,
            _ = {
              codeComponentIdentifier: p.value,
              type: c,
              scopeNodeId: Ft(a.module),
              intrinsicWidth: de($.intrinsicWidth, !0) ?? 200,
              intrinsicHeight: de($.intrinsicHeight, !0) ?? 200,
              ownerId: B,
              ownerType: E,
              group: g?.group,
              updateSaveId: g?.updateSaveId,
              namespaceId: a.module.lastPublish?.namespaceId,
              pluginId: m(h) ? h : void 0,
            };
          if (s) {
            let f = Ye(s, k, p.saveId);
            f !== void 0 && (_.propertyControlsBySaveId = Ze(l, p.saveId, f));
          }
          let b = this.componentLoader.componentForIdentifier(p.value);
          d(b, () => `Component not found in the component loader: ${p.value}`);
          let T = b.annotations ?? void 0;
          _.annotations = Jt(c) ? gt({ default: T }).default : T;
          let x = b.name ?? R;
          if (((_.title = x), l)) l.set(_);
          else {
            let f = new Ht({ id: k, ..._ });
            this.treeStore.tree.insertNode(f, F.id);
          }
        }
      }
      return j();
    });
    return (await this.installDependenciesForExternalModules(A), i);
  }
  setupPollingExternalModulesUpdates() {
    let A = r7(() => {
        this.checkForExternalModuleUpdates().catch(O.reportError);
      }, 1e4),
      j = !1,
      e = () => {
        j ||
          (A(),
          this.updatesPollId === void 0 &&
            (this.updatesPollId = window.setInterval(this.checkForExternalModuleUpdates, 6e4)));
      },
      r = () => {
        (window.clearInterval(this.updatesPollId), (this.updatesPollId = void 0));
      };
    function o() {
      return !document.hidden && navigator.onLine;
    }
    o() && e();
    function i() {
      setTimeout(() => {
        o() ? e() : r();
      }, 500);
    }
    (document.addEventListener("visibilitychange", i, { signal: this.abortSignal }),
      window.addEventListener("offline", i, { signal: this.abortSignal }),
      window.addEventListener("online", i, { signal: this.abortSignal }),
      this.abortSignal?.addEventListener("abort", () => {
        ((j = !0), r());
      }),
      (this.abortSignal = void 0));
  }
  getCodeComponentNodesForExternalModuleExport(A) {
    return (
      this.treeStore.getDataTree() ||
        d(
          !this.treeStore.tree.getService("loader"),
          "tree must be complete to query code components"
        ),
      (this.treeStore instanceof OA && this.treeStore.getDataTree()
        ? this.treeStore.query().whereClass(jj).asArray()
        : this.treeStore.getLoadedIndexes().codeComponents
      ).filter((e) => {
        if (!v(e) || (iA(e) && !e.isVectorInstance)) return !1;
        let r = G(e.codeComponentIdentifier);
        return r?.kind !== "externalModuleExport"
          ? !1
          : r.moduleId === A.moduleId && r.exportSpecifier === A.exportSpecifier;
      })
    );
  }
  getShaderNodesForExternalModuleExport(A) {
    return (
      this.treeStore.getDataTree() ||
        d(!this.treeStore.tree.getService("loader"), "tree must be complete to query shaders"),
      (this.treeStore instanceof OA && this.treeStore.getDataTree()
        ? this.treeStore.query().whereClass(Zt).asArray()
        : this.treeStore.getLoadedIndexes().shaderNodes
      ).filter((e) => {
        if (iA(e)) return !1;
        let r = G(e.shaderModuleIdentifier);
        return r?.kind !== "externalModuleExport"
          ? !1
          : r.moduleId === A.moduleId && r.exportSpecifier === A.exportSpecifier;
      })
    );
  }
  get fastRefreshEnabled() {
    return this.chromeStore.codeEditorPreviewVisible;
  }
  async updateExternalModuleComponentInstances(A) {
    let j = G(A);
    d(
      j?.kind === "externalModuleExport",
      "parsed 'ExternalModuleNodes.id' should be of kind externalModuleExport"
    );
    let { moduleId: e, exportSpecifier: r } = j,
      o = this.treeStore.tree.getNodeWithTrait(e, U),
      i = this.externalModulesDynamicInfo.get(e)?.status?.updateSaveId ?? o?.updateSaveId;
    if (!i) {
      O.warn("Could not update module", e, "latest status missing");
      return;
    }
    if (i === j.saveId) return;
    let { module: n, dependencies: s } = await this.lookUpModule(
      { moduleId: e, saveId: i },
      { includeDependencies: !0, includeStatus: !0 }
    );
    d(n.files.module, "Module file must be defined");
    let a = gA(e, i, n.files.module, r);
    try {
      (await this.loadedExternalModulesStore.ensureModuleEvaluatedInSandbox(a).catch(O.reportError),
        this.treeStore.getDataTree() ||
          (await this.scheduler.runWithFullyLoadedTreeAsync({
            name: "update_external_module_version",
            modalTitle: "Updating the module version",
          })),
        await this.updateExternalModuleNodes([{ module: n, dependencies: s, exportSpecifier: r }], {
          onTreeUpdate: () => {
            let k = this.treeStore.getDataTreeOrPartialTree();
            (n.type === "shader"
              ? this.getShaderNodesForExternalModuleExport(j).forEach((c) =>
                  c.set({ shaderModuleIdentifier: a.value })
                )
              : this.getCodeComponentNodesForExternalModuleExport(j).forEach((c) =>
                  c.set({ codeComponentIdentifier: a.value })
                ),
              Br(k, j.value, a.value),
              Ir(k, j.value, a.value));
          },
          updateSave: !0,
        }),
        this.trackExternalComponentInsert([a.value], "update"));
    } catch (k) {
      O.reportError(k);
    }
  }
  async replaceRelativeImportsWithImportMapSpecifiers(A, j, e) {
    let r = {};
    for (let i of e) {
      let n = Ij(i, A);
      if (!n) continue;
      let s;
      if (
        ((s = this.fastRefreshModules.get(n)),
        s || (s = this.modulesStorage.getModuleWithTypeSlashName(n)),
        !s)
      ) {
        O.error("Cannot resolve", i, "from", A);
        continue;
      }
      let a,
        { name: k, localId: l } = s;
      if (s.kind === "fast-refresh") a = yA(k).module;
      else {
        let { files: c } = s;
        (d(
          m(c.module),
          "Must have a module file name to build a local module import map specifier."
        ),
          (a = c.module));
      }
      r[i] = ee(l, a);
    }
    let o = await j7(j, r);
    return (d(o.ok), o.value);
  }
  getModules(A = !1) {
    let j = {};
    for (let [e, r] of this.localModules) j[e] = r;
    if (A) for (let [e, r] of this.fastRefreshModules) j[e] = r;
    return {
      dependenciesModule: this.dependenciesModule,
      modules: j,
      revision: this.revision,
      initialized: this.modulesStorage.initialized,
    };
  }
  getInitialModulePatches(A = !1) {
    let { dependenciesModule: j, modules: e } = this.getModules(A),
      r = [];
    j && r.push({ op: "add", path: [`${j.type}/${j.name}`], value: j });
    for (let o of Object.values(e)) r.push({ op: "add", path: [`${o.type}/${o.name}`], value: o });
    return r;
  }
  getPrioritizedInitialModules(A) {
    let j = new Set();
    if (!D.isOn("prioritizedModuleEvaluation") || !D.isOn("prioritizedInitialModuleEvaluation"))
      return j;
    let e = Ke(this.getActiveScope()),
      r = new Set();
    for (let o of e) A[o] && A7(this.moduleDepsGraph, o, r);
    for (let o of r) A[o] && j.add(o);
    return j;
  }
  getAllModulesAsUpdateEvent() {
    let { initialized: A, modules: j, revision: e } = this.getModules(!1),
      r = this.getPrioritizedInitialModules(j),
      o = r.size > 0 ? Object.keys(j).filter((i) => !r.has(i)) : [];
    return {
      patches: this.getInitialModulePatches(!1),
      dependentModules: o,
      prioritizedModules: Array.from(r),
      initialized: A,
      modulesReloading: this.modulesStorage.isReloadingModules(),
      revision: e,
    };
  }
  getAllFastRefreshModulesAsUpdateEvent() {
    let { initialized: A } = this.getModules(!0);
    return { patches: this.getInitialModulePatches(!0), initialized: A };
  }
  getModulesUpdatesService() {
    return {
      moduleUpdatesStream: () => this.moduleUpdatesEmitter.newStream({ replay: "latest" }),
      fastRefreshModuleUpdatesStream: () =>
        this.fastRefreshModulesEmitter.newStream({ replay: "latest" }),
    };
  }
  hasScreenModuleLoaded(A) {
    let j = Ej("screen", A, "default").value;
    return this.componentLoader.componentForIdentifier(j) !== null;
  }
  hasPendingTreeData() {
    return this.modulesStorage.hasPendingTreeData();
  }
  isReloadingModules() {
    return this.modulesStorage.isReloadingModules();
  }
  writeTreeData() {
    this.modulesStorage.writeTreeData();
  }
  postProcess(A) {
    this.modulesStorage.processTreeUpdates(A);
  }
  takeSnapshot() {
    let A = this.modulesStorage.takeSnapshot();
    return new i6(this, A);
  }
};
((w = cj(null)),
  (e6 = new WeakMap()),
  (t6 = new WeakMap()),
  (r6 = new WeakMap()),
  (o6 = new WeakMap()),
  (n6 = new WeakMap()),
  (JA = new WeakMap()),
  (ij = new WeakMap()),
  (aj = new WeakMap()),
  (mA = new WeakMap()),
  (MA = new WeakMap()),
  (RA = new WeakMap()),
  EA(w, 4, "dependenciesModule", oo, GA, e6),
  EA(w, 4, "localModules", ro, GA, t6),
  EA(w, 4, "fastRefreshModules", to, GA, r6),
  EA(w, 4, "externalModulesDynamicInfo", eo, GA, o6),
  EA(w, 4, "revision", jo, GA, n6),
  $j(w, GA));
var i6 = class {
  constructor(A, j) {
    this.modulesStorageSnapshot = j;
    ((this.dependenciesModule = A.dependenciesModule),
      (this.localModuleKeys = Array.from(A.localModules.keys())));
  }
  modulesStorageSnapshot;
  dependenciesModule;
  localModuleKeys;
  getPersistedModuleByLocalId(A) {
    return this.modulesStorageSnapshot.getPersistedModuleByLocalId(A);
  }
  getPersistedModuleByLocalIdentifier(A) {
    let j = G(A);
    if (K(j)) return this.getPersistedModuleByLocalId(j.localId);
  }
  getModuleWithTypeSlashName(A) {
    return this.modulesStorageSnapshot.getModuleWithTypeSlashName(A);
  }
};
function no(t) {
  let { newContentAsDraft: A } = t.stores.persistedUserDefaults;
  return !(!A || t.stores.publishStore.publishStatus === 1);
}
function yn(t, A, j) {
  let e = t.reactComponentForIdentifier(A.codeComponentIdentifier);
  if (!e) return;
  let r = e.properties[j.controlPropKey];
  return r ? (hj(r, A.getRawControlProp(j.controlPropKey)) ?? void 0) : void 0;
}
function On(t, A, j) {
  if (!w6(A)) return;
  let e = t.reactComponentForIdentifier(A.layoutTemplateIdentifier);
  if (!e) return;
  let r = e.properties[j.controlPropKey];
  return r ? (hj(r, A.getRawControlProp(j.controlPropKey)) ?? void 0) : void 0;
}
function Mn(t, A, j, e) {
  let i = t.getNodeParent(j).getPropertyControls(t, A)[e.controlPropKey];
  if (!i) return;
  let n = j.getControlProp(e.controlPropKey);
  return hj(i, n) ?? void 0;
}
function so(t, A, j, e) {
  if (aA(j, !0)) return On(A, j, e);
  if (P(j)) return Mn(t, A, j, e);
  if (v(j)) return yn(A, j, e);
}
function sj(t, A, j, e) {
  let r = so(t, A, j, e);
  if (!r) return;
  if (!e.ancestorControlKeyPath) return r;
  let o = xe(e.ancestorControlKeyPath);
  for (let i of o) {
    if (!r) return;
    if ((d(r.type === i.type), r.type === "object")) {
      if ((d(i.type === "object"), !AA(r.value))) return;
      r = r.value[i.key];
    } else if (r.type === "array") {
      if ((d(i.type === "array"), !VA(r.value))) return;
      r = r.value.find((n) => n.id === i.id);
    } else {
      return;
    }
  }
  return r;
}
function kj(t, A, j, e, r) {
  if (!e.ancestorControlKeyPath) {
    U6(j)
      ? j.setControlProp(e.controlPropKey, r)
      : P(j)
        ? (d(Pe(r), "Invalid control prop"), j.setControlProp(e.controlPropKey, r))
        : N(j, "Unsupported node");
    return;
  }
  d(aA(j, !0) || v(j) || P(j));
  function o(a) {
    if (a) return a.type === "array" || a.type === "object" ? { ...a } : (d(a.type === r.type), r);
  }
  let i = o(so(t, A, j, e));
  if (!i) return;
  let n = xe(e.ancestorControlKeyPath),
    s = i;
  for (let a of n) {
    if (!s) return;
    if ((d(s.type === a.type), s.type === "object")) {
      if ((d(a.type === "object", "Current control should be of type Object"), !AA(s.value)))
        return;
      let k = s.value[a.key],
        l = o(k);
      if (!l) return;
      ((s.value = { ...s.value, [a.key]: l }), (s = l));
    } else if (s.type === "array") {
      if ((d(a.type === "array", "Current control should be of type Array"), !VA(s.value))) return;
      let k = s.value.find((c) => c.id === a.id),
        l = o(k);
      if (!l) return;
      ((s.value = s.value.map((c) => (c.id === l.id ? l : c))), (s = l));
    } else {
      return;
    }
  }
  aA(j, !0) || v(j)
    ? j.setControlProp(e.controlPropKey, i)
    : P(j)
      ? (d(Pe(i), "Invalid control prop"), j.setControlProp(e.controlPropKey, i))
      : N(j, "Unsupported node");
}
function M(t, A, j, e) {
  let r = A ? A[t] : void 0,
    o = e(r, j);
  if (!(!A && !o)) {
    if (!o) {
      d(A);
      let i = Object.keys(A),
        n = i.length;
      if (n === 0 || (n === 1 && i[0] === t)) return;
      let s = { ...A };
      return (delete s[t], s);
    }
    return { ...A, [t]: o };
  }
}
function a6(t, A, j, e, r) {
  function o(i) {
    function n(s) {
      return s.name === e.type;
    }
    return {
      ...i,
      transforms: i.transforms.map((s) => (s.id !== e.transformId || !n(s) ? s : r(s))),
    };
  }
  if (bA(j) && TA(j.textContent)) {
    let i = o(j.textContent);
    j.set({ textContent: i });
    return;
  }
  if (le(j) && TA(j.formInputPlaceholder)) {
    let i = o(j.formInputPlaceholder);
    j.set({ formInputPlaceholder: i });
    return;
  }
  if (v(j)) {
    let i = sj(A, t.componentLoader, j, e);
    if (!i || (d(i.type === "string"), !TA(i.value))) return;
    let n = { ...i };
    ((n.value = o(i.value)), kj(A, t.componentLoader, j, e, n));
  }
}
function ko(t, A, j, e, r, o) {
  t.scheduler.process(() => {
    let i = t.stores.treeStore.getDataTreeOrPartialTree(),
      n = i.get(A.nodeId),
      s = A.type;
    switch (s) {
      case "string":
      case "richtext":
      case "link": {
        if (!P(n) && !v(n) && !aA(n)) return;
        let a = sj(i, t.componentLoader, n, A);
        if (!a) break;
        d(s === a.type);
        let k = { ...a },
          l = M(j.id, a.valueLocalized, "default", r);
        ((k.valueLocalized = l), kj(i, t.componentLoader, n, A, k));
        break;
      }
      case "slug": {
        if (!P(n)) return;
        let a = sj(i, t.componentLoader, n, A);
        d(a?.type === "string", "expected slug control type to be a ControlType.String");
        let k = { ...a },
          l = M(j.id, a.valueLocalized, "default", r);
        ((k.valueLocalized = l), kj(i, t.componentLoader, n, A, k));
        break;
      }
      case "image": {
        if (!P(n) && !v(n) && !aA(n)) return;
        let a = sj(i, t.componentLoader, n, A);
        if (!a) break;
        d(s === a.type);
        let k = { ...a };
        if (e === "imageSrc" || e === "all") {
          let l = M(j.id, a.valueLocalized, "imageSrc", r);
          k.valueLocalized = l;
        }
        if (e === "default" || e === "all") {
          let l = M(j.id, a.altLocalized, "default", r);
          k.altLocalized = l;
        }
        kj(i, t.componentLoader, n, A, k);
        break;
      }
      case "enum": {
        if (!Y(n)) return;
        let a = n.variables.map((k) => {
          if (k.id !== A.variableId) return k;
          d(k.type === A.type);
          let l = k.cases.map((c) => {
            if (c.id !== A.enumCaseId) return c;
            let $ = M(j.id, c.nameLocalized, "default", r);
            return { ...c, nameLocalized: $ };
          });
          return { ...k, cases: l };
        });
        n.set({ variables: a });
        break;
      }
      case "RootNode":
      case "WebPageNode": {
        if (!zt(n)) return;
        let a = A.key === "title" ? "titleLocalized" : "descriptionLocalized",
          k = n[a],
          l = M(j.id, k, "default", r);
        n.set({ [a]: l });
        break;
      }
      case "RichTextNode": {
        if (!bA(n)) return;
        let a = M(j.id, n.htmlLocalized, "default", r);
        (n.set({ htmlLocalized: a }), lt(n) && Ee(t) && Yt(t, Wt(t, [n.id])).catch(z));
        break;
      }
      case "FrameNode": {
        if (!_6(n)) return;
        if (e === "imageSrc" || e === "all") {
          let a = M(j.id, n.fillImageLocalized, "imageSrc", r);
          n.set({ fillImageLocalized: a });
        }
        if (e === "default" || e === "all") {
          let a = M(j.id, n.altAttributeLocalized, "default", r);
          n.set({ altAttributeLocalized: a });
        }
        break;
      }
      case "nodeLink": {
        if (!kt(n)) break;
        let a = M(j.id, n.linkLocalized, "default", r);
        n.set({ linkLocalized: a });
        break;
      }
      case "prefix":
      case "suffix": {
        if (!n) break;
        a6(t, i, n, A, (a) => {
          let k = { ...a },
            l = M(j.id, a.valueLocalized, "default", r);
          return ((k.valueLocalized = l), k);
        });
        break;
      }
      case "convertFromBoolean": {
        if (!n) break;
        a6(t, i, n, A, (a) => {
          let k = { ...a };
          return (
            A.property === "truthy" &&
              (k.truthyLocalized = M(j.id, a.truthyLocalized, "default", r)),
            A.property === "falsy" && (k.falsyLocalized = M(j.id, a.falsyLocalized, "default", r)),
            A.property === "fallback" &&
              a.fallback &&
              (k.fallback = {
                ...a.fallback,
                valueLocalized: M(j.id, a.fallback.valueLocalized, "default", r),
              }),
            k
          );
        });
        break;
      }
      case "convertFromEnum": {
        if (!n) break;
        a6(t, i, n, A, (a) => {
          let k = { ...a };
          return A.isDefaultCase
            ? ((k.defaultLocalized = M(j.id, a.defaultLocalized, "default", r)), k)
            : ((k.cases = a.cases.map((l) => {
                if (l.id !== A.caseId) return l;
                let c = { ...l },
                  $ = M(j.id, l.toLocalized, "default", r);
                return ((c.toLocalized = $), c);
              })),
              k);
        });
        break;
      }
      case "FormPlainTextInputNode": {
        if (!le(n)) return;
        n.set({
          formInputPlaceholderLocalized: M(j.id, n.formInputPlaceholderLocalized, "default", r),
        });
        break;
      }
      case "FormSelectNode": {
        if (!ce(n) || !n.formSelectOptions) return;
        let { optionId: a } = A,
          k = n.formSelectOptions.map((l) => {
            if (l.id !== a) return l;
            let c = M(j.id, l.titleLocalized, "default", r);
            return { ...l, titleLocalized: c };
          });
        n.set({ formSelectOptions: k });
        break;
      }
      case "formSelectGenericOption": {
        if (!ce(n)) return;
        let { key: a } = A;
        switch (a) {
          case "allItemsLabel": {
            let k = M(j.id, n.allItemsLabelLocalized, "default", r);
            n.set({ allItemsLabelLocalized: k });
            break;
          }
          case "booleanTrueLabel": {
            let k = M(j.id, n.booleanTrueLabelLocalized, "default", r);
            n.set({ booleanTrueLabelLocalized: k });
            break;
          }
          case "booleanFalseLabel": {
            let k = M(j.id, n.booleanFalseLabelLocalized, "default", r);
            n.set({ booleanFalseLabelLocalized: k });
            break;
          }
          default:
            N(a);
        }
        break;
      }
      case "RouteSegmentNode": {
        if (!T6(n)) return;
        let a = n.segmentLocalized,
          k = M(j.id, a, "default", r);
        n.set({ segmentLocalized: k });
        break;
      }
      default:
    }
  }, o);
}
function vn(t, A, j, e, r) {
  (d(
    Ne(A) && typeof A.richTextValue != "string",
    "RichTextDocument localized values are only supported for JSON-backed rich text control prop sources"
  ),
    t.scheduler.process(() => {
      let o = t.stores.treeStore.getDataTreeOrPartialTree(),
        i = o.get(A.nodeId);
      if (!P(i) && !v(i) && !aA(i)) return;
      let n = sj(o, t.componentLoader, i, A);
      if (!n) return;
      d(n.type === "richtext");
      let s = structuredClone(n);
      ((s.valueLocalized = M(j.id, n.valueLocalized, "default", e)),
        kj(o, t.componentLoader, i, A, s));
    }, r));
}
function io(
  t,
  A,
  j,
  e,
  r,
  {
    needsReview: o,
    generatedByAI: i,
    pluginId: n,
    pluginMode: s,
    ignoreTracking: a,
    trackingSource: k,
  }
) {
  let l = { value: t, hash: Pn(A, j), lastEdited: Date.now() };
  o && (l.needsReview = !0);
  let c = R6(r?.value, t);
  return (
    (i || (c && r?.generatedByAI)) && (l.generatedByAI = !0),
    n || UA(s)
      ? (d(n && UA(s), "Cannot set only pluginId or pluginMode"),
        (l.pluginId = n),
        (l.pluginMode = s))
      : c &&
        r?.pluginId &&
        ((l.pluginId = r.pluginId), r.pluginMode && (l.pluginMode = r.pluginMode)),
    a ||
      L(r ? "localized_value_update" : "localized_value_create", {
        code: e.code,
        valueLength: typeof t == "string" ? t.length : 0,
        generatedByAI: !!l.generatedByAI,
        source: k,
      }),
    l
  );
}
function Pn(t, A) {
  switch (A) {
    case "default":
      return t.hash;
    case "imageSrc":
      return (d(Cj(t)), t.imageHash);
    default:
      N(A);
  }
}
function Tn(t, A) {
  if (!A.draft) return;
  let j = t.stores.localizationStore.progressPerLocale[A.id];
  !W(j) ||
    j === 1 ||
    t.scheduler.runBeforeNextFrame(() => {
      t.stores.localizationStore.progressPerLocale[A.id] === 1 &&
        (t.stores.modalStore.stack.some(({ type: r }) => r === "BatchTranslate") ||
          t.stores.modalStore.set({ type: "LocaleReady", activeLocale: A, source: "automatic" }));
    });
}
function Cn(t, A) {
  if (!t.stores.chromeStore.isPreviewingLocaleOnCanvas) return;
  let j = t.stores.treeStore.getDataTreeOrPartialTree().get(A.nodeId);
  if (!Y(j) && !P(j)) return;
  let e = Y(j) ? j.id : j.parentid;
  e && t.stores.codeGenerationStore.updateComponent(e).catch(z);
}
function ao(t, A, j) {
  Ee(t) && (Tn(t, j), Cn(t, A));
}
function lo(t, A, j, e, r) {
  let {
    needsReview: o,
    generatedByAI: i,
    pluginId: n,
    pluginMode: s,
    ignoreTracking: a,
    target: k = "default",
    trackingSource: l,
    preserveExistingTextType: c,
    eventType: $,
  } = r;
  if (
    (d(
      typeof A == "string" || A === null || (Ne(j) && typeof j.richTextValue != "string"),
      "RichTextDocument localized values are only supported for JSON-backed rich text control prop sources"
    ),
    A !== null && typeof A != "string")
  ) {
    (vn(
      t,
      j,
      e,
      (B) =>
        io(A, j, k, e, B, {
          needsReview: o,
          generatedByAI: i,
          pluginId: n,
          pluginMode: s,
          ignoreTracking: a,
          trackingSource: l,
        }),
      $
    ),
      ao(t, j, e));
    return;
  }
  (ko(
    t,
    j,
    e,
    k,
    (B) => {
      let E = io(A, j, k, e, B, {
        needsReview: o,
        generatedByAI: i,
        pluginId: n,
        pluginMode: s,
        ignoreTracking: a,
        trackingSource: l,
      });
      if (
        (k === "imageSrc" &&
          ("imageFocalPoint" in r
            ? r.imageFocalPoint && (E.imageFocalPoint = r.imageFocalPoint)
            : B?.imageFocalPoint && (E.imageFocalPoint = B.imageFocalPoint)),
        c)
      )
        if (k === "default") {
          let p = j.localizedValues[e.id],
            R = p?.value ?? null;
          (d(R === A, "The text type can only be preserved when the source value is unchanged"),
            p?.type && (E.type = p.type));
        } else k === "imageSrc" || N(k);
      else Hr(j) && typeof A == "string" && Vr(j) && (E.type = "rich-text");
      return E;
    },
    $
  ),
    ao(t, j, e));
}
function co(t, A) {
  let j = t.localizedValues[A.id];
  return j ? j.value : null;
}
function o$(t, A, j, e) {
  let r = "default",
    o = co(A, j),
    i = A.localizedValues[j.id];
  if (Cj(A)) {
    let n = A.imageLocalizedValues[j.id];
    n && ((r = "imageSrc"), (o = n.value), (i = n));
  }
  (lo(t, o, A, j, {
    needsReview: !0,
    ignoreTracking: !0,
    target: r,
    trackingSource: e,
    preserveExistingTextType: !0,
  }),
    L("localized_value_request_review", {
      code: j.code,
      valueLength: typeof o == "string" ? o.length : 0,
      generatedByAI: !!i?.generatedByAI,
      source: e,
    }));
}
function n$(t, A, j, { trackingSource: e, ignoreTracking: r }) {
  let o = "default",
    i = co(A, j),
    n = A.localizedValues[j.id];
  if (Cj(A)) {
    let s = A.imageLocalizedValues[j.id];
    s && ((o = "imageSrc"), (i = s.value), (n = s));
  }
  (lo(t, i, A, j, {
    ignoreTracking: !0,
    target: o,
    trackingSource: e,
    preserveExistingTextType: !0,
  }),
    r ||
      L("localized_value_approve", {
        code: j.code,
        valueLength: typeof i == "string" ? i.length : 0,
        generatedByAI: !!n?.generatedByAI,
        source: e,
      }));
}
function i$(t, A, j, { target: e, ignoreTracking: r, trackingSource: o, eventType: i }) {
  ko(
    t,
    A,
    j,
    e,
    (n) => {
      let s = n?.value;
      r ||
        L("localized_value_clear", {
          code: j.code,
          valueLength: typeof s == "string" ? s.length : 0,
          generatedByAI: !!n?.generatedByAI,
          source: o,
        });
    },
    i
  );
}
function $o(t, A, j, e) {
  A.stores.canvasStore.invalidateTransformUntilRendered(() => {
    try {
      (A.stores.scopeStore.select(t, e), A.stores.codeEditorStore.closeEditor());
    } catch {
      (L("open_primary_component_fail", {}),
        X({
          type: "add",
          variant: "error",
          key: "invalid-deep-link",
          primaryText: "Primary component",
          secondaryText: "has been deleted.",
          icon: "error",
          duration: 1e4,
        }));
      return;
    }
    if (!j) return;
    let r = A.tree.getNode(j);
    r &&
      A.tree.getScopeNodeFor(r)?.id === t &&
      (A.stores.selectionStore.set(r.id),
      A.stores.canvasStore.zoomToCenter(ot(A.tree, r), { animated: !1, maxZoom: 1 }));
  });
}
function Dn(t, A) {
  let j = t.stores.insertSidebarStore.getEditBehavior(A.moduleId),
    e = xj(t.stores.treeStore.tree, A),
    r = t.componentLoader.componentForIdentifier(A.value)?.annotations;
  return (e && j === "block") || u6(r, "framerDisableUnlink");
}
function Eo(t, A, j, e) {
  let r = G(t),
    o = Mj(A, "canDesign"),
    { includeAccelerator: i = !0 } = e;
  if (K(r) && (r.type === "canvasComponent" || r.type === "screen" || r.type === "vector"))
    return [
      {
        label: o ? "View" : "Edit",
        accelerator: i ? Dj.fakeSelectChildren : void 0,
        click: () => {
          if (r.type === "vector") {
            let c = A.stores.modulesStore
              .forType("vector")
              .getByStableName(r.localIdName)
              ?.annotations(null, "default")?.framerVector;
            if (!qA(c)) return;
            let { set: $ } = c;
            $o($.id, A, r.localIdName, e);
            return;
          }
          let [, l] = r.localId.split("/");
          l && $o(l, A, j, e);
        },
      },
    ];
  if (q(r) && r.kind === "externalModuleExport") {
    if (e.node?.isVectorInstance)
      return mr(A, t)
        ? oA()
        : [
            {
              label: "Edit",
              accelerator: i ? Dj.fakeSelectChildren : void 0,
              click: () => M7(A, r.moduleId, e.node?.id),
              enabled: !o,
            },
          ];
    let l = A.stores.insertSidebarStore.getEditBehavior(r.moduleId),
      c = A.stores.treeStore.tree.getNode(r.moduleId),
      B = e.externalModuleType === "canvasComponent" && c && c.ownerId === "1h0OTH9KlHTd4pyc9CLt";
    if (Dn(A, r)) return oA();
    if (!Je(e.externalModuleType)) return oA();
    let { node: E, withUnlinkModal: p, externalModuleType: R } = e;
    return E && l === "autoDetach"
      ? [
          {
            label: "Detach Instance",
            enabled: !o,
            click: async () => {
              (A.beginUndoGroup(),
                cr(
                  {
                    key: "auto-detach",
                    progress: {
                      variant: "progress",
                      primaryText: "Detaching component",
                      secondaryText: "instance\u2026",
                    },
                    error: {
                      variant: "error",
                      primaryText: "Failed to detach",
                      secondaryText: "component instance.",
                    },
                  },
                  async () => {
                    await xn(E, A, r, R);
                  }
                ).catch(z),
                A.endUndoGroup());
            },
          },
        ]
      : B
        ? [
            {
              label: "Edit",
              accelerator: i ? Dj.fakeSelectChildren : void 0,
              click: () => {
                O7(A, { identifier: r, moduleType: R, instance: E, enterIsolation: !0 }).catch(z);
              },
              enabled: !o,
            },
          ]
        : [
            {
              label: "Unlink",
              click: () => {
                Ve(A, {
                  identifier: r,
                  moduleType: R,
                  instance: E,
                  withUnlinkModal: p,
                  enterIsolation: !0,
                }).catch(z);
              },
              enabled: !o,
            },
          ];
  }
  let n = Jj(A.componentLoader, t);
  if (!n || n.type !== "component" || n.depth !== 0) return oA();
  let s = e.keepHistory
      ? void 0
      : () => {
          A.stores.scopeStore.goBack();
        },
    a = vj(A.stores.treeStore, A.stores.modulesStore, A.stores.pluginStore, r);
  return [
    ...(a && Sr(A.stores.modulesStore, r) && !o && K(t)
      ? [
          {
            label: br(a),
            click: () => {
              (s?.(), wj(A, t, a));
            },
          },
        ]
      : []),
    {
      label: `${o ? "View" : "Edit"} Code`,
      click: () => {
        (s?.(), A.stores.codeEditorStore.editFile(n.file));
      },
      enabled: K(t),
    },
  ];
}
async function xn(t, A, j, e) {
  let r = new SA(),
    o = await He(A, {
      identifier: j,
      moduleType: e,
      instance: t,
      renamedIds: r,
      isAutoDetach: !0,
      enterIsolation: !1,
    });
  if (!o) return null;
  let { node: i } = o;
  return jA(i)
    ? A.scheduler.processWhenReadyAsync(() => {
        let n = Pj.detachSmartComponentOrWebPageInstance(A, t.draftOrCurrent(), i, r);
        return (n && A.stores.selectionStore.set(n.id), n);
      })
    : null;
}
function Bo(t) {
  return t?.find((j) => j.enabled !== !1)?.click;
}
function Nn(t, A, j) {
  return Eo(t, A, null, j);
}
function S$(t, A, j) {
  return Bo(Nn(t, A, j));
}
function qn(
  t,
  A,
  { withUnlinkModal: j = !0, externalModuleType: e, activateContentPanel: r } = {}
) {
  let o = qt(A.tree, t);
  return Eo(t.codeComponentIdentifier, A, o, {
    keepHistory: !0,
    node: t,
    withUnlinkModal: j,
    externalModuleType: e,
    activateContentPanel: r,
  });
}
function y$(t, A, j = {}) {
  return Bo(qn(t, A, j));
}
function s6(t, A, j) {
  if (!t) return !1;
  let e = t.get(A);
  return e === void 0 ? !1 : typeof e == "string" ? e !== j : e.size > 1 || !e.has(j);
}
function Kn(t, A, j) {
  if (A.type === "slug") return (e) => s6(t.get(A.id), e, j);
}
function uo(t, A, j, e, r, o, i) {
  if (r === "associatedVariable" && t === "string") {
    if (j && m(j.value)) return o && j.value === "" ? { type: "required" } : po(j, A);
    if (o && !e) return { type: "required" };
    if (!e || !be(A)) return;
    let { maxLength: a } = A;
    return W(a) && e.length > a ? { type: "max-length", message: Tj(e.length, a) } : void 0;
  }
  if (t === "slug") {
    let a;
    if ((j && oe(j.value) ? (a = QA(j.value)) : e && (a = QA(e)), !a)) return { type: "required" };
    if ((d(i, "isSlugTaken is required for slug fields"), i(a)))
      return { type: "slug-taken", message: De() };
  }
  let n = Ln(j, A, o);
  if (n) return n;
  let s = wn(j, A, o);
  return s || po(j, A);
}
function Q$(t, A, j = !1) {
  if (t)
    switch (t.type) {
      case "required":
        return Lr(A.title, j);
      case "slug-taken":
        return De(j);
      default:
        return t.message;
    }
}
function Ln(t, A, j) {
  if (!j) return;
  let { type: e } = A;
  switch (e) {
    case "link":
      return t
        ? (d(t.type === "link"),
          !t.value || (t.value.type === "url" && t.value.url === "")
            ? { type: "required" }
            : void 0)
        : { type: "required" };
    case "richtext":
      return t
        ? (d(t.type === "richtext"),
          d(!XA(t.value), "Dynamic values are not supported within the CMS."),
          qr(t.value) ? { type: "required" } : void 0)
        : { type: "required" };
    case "image":
    case "responsiveimage": {
      let r = _t(A) && !!A.__defaultAssetReference;
      return t
        ? (d(t.type === "image"), !t.value && !r ? { type: "required" } : void 0)
        : r
          ? void 0
          : { type: "required" };
    }
    case "string":
      return t
        ? (d(A.type === "string"), !oe(t.value) ? { type: "required" } : void 0)
        : A.defaultValue
          ? void 0
          : { type: "required" };
    case "collectionreference": {
      let r = yt(A, t);
      return _A(r) ? { type: "required" } : void 0;
    }
    case "multicollectionreference": {
      let r = Ot(A, t);
      return XA(r) ? void 0 : _A(r) || r.length === 0 ? { type: "required" } : void 0;
    }
    case "file":
    case "date":
      return t?.type === e && !!t.value ? void 0 : { type: "required" };
    case "array": {
      if (!t) return { type: "required" };
      d(t.type === "array");
      let r = St(A, t);
      return (
        d(!XA(r), "Dynamic values are not supported within the CMS."),
        (Ce(A) ? Te(r) : r).length === 0 ? { type: "required" } : void 0
      );
    }
    default:
      return;
  }
}
function po(t, A) {
  if (!be(A)) return;
  let { maxLength: j } = A;
  if (W(j))
    switch (A.type) {
      case "string": {
        d(!t || t.type === "string");
        let e = A.defaultValue ?? "",
          r = t?.value && m(t.value) ? t.value : e;
        return r.length > j ? { type: "max-length", message: Tj(r.length, j) } : void 0;
      }
      case "richtext": {
        if (!t || XA(t.value)) return;
        let e = Er(t.value);
        return e.length > j ? { type: "max-length", message: Tj(e.length, j) } : void 0;
      }
      default:
        N(A);
    }
}
function z$(t, A, j) {
  return uo(t.type, A, j, void 0, void 0, he(t), void 0);
}
function wn(t, A, j) {
  if (!Ct(A) || (UA(t?.value) && !at(t?.value)) || TA(t?.value)) return;
  let r = Ce(A) ? Te(t?.value) : (t?.value ?? []);
  if (W(A.minCount) && r.length < A.minCount)
    return !j && r.length === 0 ? void 0 : { type: "min-count", message: wr(A, A.minCount) };
  if (W(A.maxCount) && r.length > A.maxCount)
    return { type: "max-count", message: Jr(A, A.maxCount) };
}
function Jn(t, A, j, e, r, o, i) {
  let n,
    s,
    a = o7(t, r, i);
  return (
    (ge(j) || _j(j)) && (n = Qt(j, e, A)),
    ge(j) && (s = j.fallbackValue),
    uo(j.type, r, a, n, s, he(j), o)
  );
}
function Hn(t) {
  return t.variables.filter((A) => A.type !== "divider");
}
function W$(t, A, j, e) {
  let r = j.getPropertyControls(t, A),
    o = Hn(j),
    i = j.getSlugOwners();
  return o.filter((n) => {
    let s = r[n.id];
    if (!s) return !1;
    let a = Kn(i, n, e.id);
    return !!Jn(t, e, n, j.variables, s, a, e.getControlProp(n.id));
  });
}
function Go(t, A) {
  let j = QA(t);
  return j.length ? KA(j, A, { withDash: !0 }) : "";
}
function id(t, A, j, e) {
  for (let r of t.variables) {
    if (r.type === "divider" || !_j(r)) continue;
    let o = e?.get(r.id) ?? A.getControlProp(r.id);
    if ((!o || !m(o.value) || !o.value.length) && r.associatedStringVariable) {
      let n = e?.get(r.associatedStringVariable) ?? A.getControlProp(r.associatedStringVariable);
      if (n && m(n.value)) {
        let s = Go(n.value, (a) => s6(j.get(r.id), a, A.id));
        if (s.length)
          return (
            A.setControlProp(r.id, { type: "string", value: s }),
            { changedControlProp: r.id }
          );
      }
    }
  }
  return !1;
}
var Vn = /<([a-z1-9]+)/gi;
function Ed(t) {
  let A = new Set();
  return (Un(t, A), A);
}
function Un(t, A) {
  let j = t.matchAll(Vn);
  for (let [, e] of j) e && A.add(e);
  return A;
}
function Id(t) {
  t.engine.scheduler.processWhenReady(() => {
    Xn(t);
  });
}
function Xn({ collectionId: t, engine: A, saveAction: j, source: e }) {
  let r = A.tree.getNodeWithTrait(t, Y);
  d(r, "Collection needs to exist to allow creation of new item");
  let o = A.stores.selectionStore.ids[0],
    i = A.tree.getNodeWithTrait(o, Y);
  (i && i.id !== r.id && d(j, "saveAction is required when creating a nested private draft item"),
    Qn({ collection: r, engine: A, saveAction: j }),
    L("collection_record_create", { source: e }));
}
function Qn({ collection: t, engine: A, saveAction: j }) {
  let e = zn(A, t, 0);
  A.stores.contentManagementStore.openPrivateDraftCollectionItem({
    collectionId: t.id,
    itemId: e.id,
    saveAction: j,
  });
}
function zn(t, A, j) {
  let e = t.stores.sessionStore.user.id,
    r = Wn(A, e);
  if (r) return r;
  let o = Xt.create({ privateToUserId: e, ...Vt(t.tree.root.locales ?? oA()) });
  return (t.stores.scopeStore.insertNode(o, A.id, j), Zn(A, o), no(t) && o.set({ isDraft: !0 }), o);
}
function Wn(t, A) {
  let j,
    e = 0;
  for (let r of t.children) {
    if (r.privateToUserId !== A) continue;
    let o = Yn(r);
    (!j || o > e) && ((j = r), (e = o));
  }
  return j;
}
function Yn(t) {
  if (!t.createdAt) return 0;
  let A = new Date(t.createdAt).getTime();
  return Number.isNaN(A) ? 0 : A;
}
function Zn(t, A) {
  let j = Ai(A);
  for (let e of t.variables)
    e.type === "date" &&
      A.setControlProp(e.id, { type: "date", value: k6(j, e.options?.displayTime) });
}
function Ai(t) {
  if (!t.createdAt) return new Date();
  let A = new Date(t.createdAt);
  return Number.isNaN(A.getTime()) ? new Date() : A;
}
function k6(t, A) {
  return A
    ? new Date(
        Date.UTC(t.getFullYear(), t.getMonth(), t.getDate(), t.getHours(), t.getMinutes(), 0, 0)
      ).toJSON()
    : new Date(Date.UTC(t.getFullYear(), t.getMonth(), t.getDate())).toJSON();
}
function yd({ variables: t, collectionItem: A }) {
  if (!A.hasData()) return !1;
  for (let j of t) {
    if (j.type === "divider") continue;
    let e = A.getControlProp(j.id);
    if (!(!e || !Ut(e.value)) && !ji(A, e, j)) return !0;
  }
  return !1;
}
function ji(t, A, j) {
  if (j.type !== "date" || A.type !== "date" || !m(A.value) || !t.createdAt) return !1;
  let e = new Date(t.createdAt);
  return Number.isNaN(e.getTime()) ? !1 : A.value === k6(e, j.options?.displayTime);
}
export {
  Qi as a,
  R7 as b,
  No as c,
  Ko as d,
  Ia as e,
  ga as f,
  ni as g,
  ii as h,
  ai as i,
  si as j,
  Jj as k,
  _7 as l,
  Ii as m,
  gi as n,
  $7 as o,
  d7 as p,
  Je as q,
  He as r,
  Ve as s,
  O7 as t,
  $o as u,
  Dn as v,
  xn as w,
  Nn as x,
  S$ as y,
  qn as z,
  y$ as A,
  bs as B,
  zo as C,
  Ss as D,
  ys as E,
  Os as F,
  Ms as G,
  Wo as H,
  Xe as I,
  Si as J,
  u7 as K,
  G7 as L,
  yi as M,
  J7 as N,
  dk as O,
  Gn as P,
  OA as Q,
  GA as R,
  Li as S,
  _o as T,
  wi as U,
  no as V,
  Id as W,
  Xn as X,
  zn as Y,
  Wn as Z,
  Zn as _,
  yd as $,
  s6 as aa,
  Kn as ba,
  uo as ca,
  Q$ as da,
  Ln as ea,
  po as fa,
  z$ as ga,
  wn as ha,
  Jn as ia,
  Hn as ja,
  W$ as ka,
  Go as la,
  id as ma,
  Ed as na,
  Un as oa,
  lo as pa,
  o$ as qa,
  n$ as ra,
  i$ as sa,
};
//# sourceMappingURL=chunk-ZNORD66S.mjs.map
